// 自動生成。手で書き換えない。
// 作り直す: node scripts/generate-about.mjs（古いまま commit すると releasesGenerated.test.ts が落ちる）
import type { EmbeddedReleases } from '../about.js';

export const EMBEDDED_RELEASES: EmbeddedReleases = {
  "desktop": {
    version: "0.30.2",
    hasOlder: true,
    releases: [
    { version: "0.30.2", notes: "### 修正\n\n- **窓を撮る頼みが重なると、アプリごと落ちていたのを直した**。撮るのに使う仕組みは、同じプロセスの中で 2 つ同時に立てられない。断りが返るのではなく**その場で止まる**ので、呼んだ側には何も返らずアプリが消える。窓は何枚でも開けるし、外からの頼みも同時に来るので、重なりは普通に起こる。撮るのは 1 度に 1 枚ずつにして、重なった分は順番を待つようにした。\n\n- **下見の面が、何をどこから出しているのかを名乗るようになった**。web モードでは待ち受けが 2 つ立つ——アプリが組み上げて出しているものと、プロジェクトが `md-business.yml` で宣言した自前のものと。どちらを映すかは開いている文書で変わる（業務文書は前者、サイトの部品は後者）ので、面を見ただけではどちらが出ているのか分からなかった。見出しに出どころと在り処を書くようにした。**この面に出ていないほうの待ち受けも添える**——片方しか書かないと、もう一方が立っていないのか別のところに出ているのかが読めず、次に何をすればいいか決められない。**出しているものは変えていない**（業務文書はこれまでどおりアプリの中で組んだものを出す）。" },
    { version: "0.30.1", notes: "### 修正\n\n- **外から「このファイルを開いて」と頼んでも、サイトの部品だけ何も起きなかったのを直した**。一覧には `.astro` や `.ts` が並ぶのに、外から開く受け口だけが Markdown と画像しか受け取らず、それ以外は黙って捨てていた。頼んだ側には成功と返るので、アプリが「文書を選択してください」のまま止まっていても、開いたつもりで先へ進んでしまう。受け口を一覧と同じ範囲まで広げた。あわせて、**無いファイルを頼まれたときはアプリを起こす前に断る**ようにした。起こすだけの口はアプリが受け取ったかを返せないので、打ち間違いでも成功として返っていた。\n\n- **タブを掴んでも並べ替えられず、「禁止」の印が出ていたのを直した**。0.16.0 で並べ替えを付けてから、Windows ではずっと落とせないままだった。原因は画面側ではなく窓の設定にある。窓には「机の上からファイルを落とせる」受け口が既定で付いていて、これがあると **WebView2 が面の上のドラッグをすべて横取りする**。画面の中だけで完結する並べ替えも、外から来たものと区別されずに奪われるので、掴めるのに落とせない。このアプリは机の上からのドロップを使っていないので、受け口ごと切った。**開いている窓すべてに効く**（2 枚目以降の窓は最初の窓の設定を写して作る）。設定が戻ると同じ壊れ方に戻るので、切れていなければ落ちるテストで留めてある。" },
    { version: "0.30.0", notes: "### 追加\n\n- **2 つのフォルダを、2 つの窓で並べて開けるようになった**。ファイルメニューの「別の窓を開く」から増やす。窓ごとに開くフォルダは 1 つで、見張り・下見・エージェントの受け口も窓ごとに 1 本ずつ持つ。作りかけのサイトを直しながら、隣の窓で検証シートを埋める、といった使い方ができる。窓ごとに前回のフォルダを覚えるので、次に起ち上げたときも同じ 2 つが並ぶ。**タスクバーと Alt+Tab の項目名は、開いているフォルダの名前になる**（同じ名前が 2 つ並ぶと、どちらがどちらか分からない）。文書名ではなくフォルダ名にしたのは、文書を切り替えるたびに項目名が変わると目で追えなくなるため。同時に開けるのは 8 つまでで、上限に達すると「別の窓を開く」が灰色になる（窓ごとに常駐するものがあるので、際限なく開けると機械が持たない。押しても何も起きない項目にすると、開かなかったのが不具合なのか上限なのか分からない）。\n\n- **同じフォルダを 2 つの窓で開こうとすると断るようにした**。窓ごとに 1 本ずつ持つものが、同じ場所を取り合うため。とくにエージェント向けの接続情報はフォルダに書き出すもので、窓ごとに違う待ち受け先が入る。後から開いた窓が先の窓の分を上書きすると、**先の窓につないだつもりのエージェントが、黙って別の窓のフォルダを触る**。断ると同時に、既にそのフォルダを開いている窓を前に出す（畳まれていると、どこで開いているのか探しに行けない）。親フォルダと子フォルダは指しているところが違うので、そのまま開ける。\n\n- **アプリの窓を、エージェントから撮って画像で受け取れるようになった**（MCP の `capture_window`）。エージェントは文書の中身なら読めるが、**アプリが実際にどう描いているかは読めない**。列がはみ出している・行の色が付いていない・ダイアログが裏に隠れている、といった見た目の壊れ方は、ファイルを何度読んでも出てこない。撮るのは画面ではなくアプリが抱えている面なので、**手前に重なっている他のアプリは写らず、畳んでいても撮れる**（画面を撮ると、そのとき前に出ていた別のアプリの中身まで一緒に渡ることになる）。写るのはこのアプリの窓だけで、他のアプリや机の上は撮れない。既定では長辺 1400 まで縮める（200〜4000 の間で指定できる。小さい窓は引き伸ばさない）。返るのは**利用者が今見ている画面そのもの**なので、Issue や PR のような外から見えるところへ貼る前に本人へ確かめる、と説明にも書いてある。" },
    { version: "0.29.0", notes: "### 追加\n\n- **フォルダが「サイト」を名乗れるようになった**。`md-business.yml` に `mode: web` と書くと、そのフォルダでは Markdown だけでなく HTML・CSS・JavaScript も一覧に出て、下見が本物のページとして動く。**書いてあるのは宣言であって、実行の許可ではない**。動かすかどうかは、この機械で人が 1 回押して決める。フォルダを配った先へその許可は付いていかない（許可まで一緒に渡ると、受け取った側は中身を見る前に script が動いていることになる）。宣言はアプリから書けるし、取り下げられる。許可も後から取り消せる。**この 2 つは別々に消える**（取り下げても許可は残り、許可を消しても宣言は残る）。片方が他方を消す作りにすると、配布物を直したのに手元だけ動き続ける／手元で断ったのに配布物は宣言したまま、のどちらかが必ず起きる。心当たりの無いフォルダでは許可しないでほしい、と尋ねる画面にも書いてある。\n\n- **下見で script を動かすかを、フォルダごとの同意で決めるようにした**。動かす側にしたページには、読み込んでよい出どころの指定を付ける（宣言に書いていない出どころからは読み込ませない）。**書き出したフォルダにも同じ指定を焼き込む**。手元の下見だけ厳しくしても、出したものが緩ければ守れているのは自分の画面だけになる。\n\n- **外で動いている待ち受けを、同じ面に映せるようになった**。`web:` の下に `devServer: http://localhost:4321` と書くと、下見の面がそこを映す。**アプリはその待ち受けを起動しない**。起動まで持つと、フォルダを開いただけでプロジェクトのコマンドが走ることになり、上の同意が意味を失う。立てるのも止めるのも今までどおり手元の端末で、アプリは映すだけ。宛先はこの機械の中（`localhost`）だけ受け付ける。\n\n- **サイトの部品を、エージェントからも扱えるようになった**（MCP）。書く口・読み返す口・一覧する口と、宣言だけを書く口を足した。書いたものは開いている画面にそのまま届く（開き直さなくても下見が変わる）。アプリの側では**読み取り専用で開く**（直す場所は元のファイルで、写しを直しても戻らない）。\n\n- **本文に ```` ```data ```` の囲みを置くと、別ファイルの表をその場に描くようになった**。`source:` に表の場所を書く。数字を本文へ写すと、表を直したときに本文だけ古いまま残る。指定が通らないときは黙って空にせず、囲みの何行目の何が読めなかったかを出す。\n\n### 修正\n\n- **宣言を書き換えても、一覧が前のままだったのを直した**。\n- **サイトの部品を、中身が書き換わっただけのときに組み直さないようにした**。ライブ更新のたびに全部を組み直していた。" },
    { version: "0.28.0", notes: "### 追加\n\n- **Markdown の脚注（`[^1]`）が注釈として読めるようになった**。脚注は本文に肩番号だけを残して、本文を文書の末尾へ畳む。紙ならページを戻れば済むが、画面では**末尾まで目を移した時点で読んでいた場所を見失う**。表や図の途中だと、戻ってきても同じ行を探すところからやり直しになる。肩番号にカーソルを合わせると、その場に本文が出るようにした。末尾の一覧は今までどおり残す（**一覧のほうが正で、その場に出るのは同じ本文の写し**。印刷や読み上げには一覧だけが出る）。読み上げには二度読ませない（肩番号は既に末尾の本文を指しているので、写しまで読ませると同じ文が続けて 2 回読まれる）。**紙では戻る記号（`↩`）を出さない**。画面では一覧から本文へ帰る道になるが、紙の上では押せない記号が番号の隣に並ぶだけで、意味を探させるぶん読みにくくなる。体裁は本文より小さく、上に区切りを引いて、章見出しの装飾は引き継がせない（注釈は章ではない）。\n\n### 修正\n\n- **末尾の注釈の見出しが、日本語の文書にも英語で `Footnotes` と刷られていたのを直した**。脚注の既定の見出しは英語で、しかも「読み上げにだけ残して目には見せない」前提の指定が付いている。その指定に当たるスタイルをこちらは持っていないので、**隠れないまま英語の見出しだけが出ていた**。隠すのではなく、文書の言語の見出しとして出す（注釈 / Notes / 注释 / 주석）。戻る記号の読み上げラベルも同じ言語で出す。目に映るのは記号だけなので気づきにくいが、読み上げるとそこだけ英語が挟まる。" },
    ],
  },
  "chrome-extension": {
    version: "0.6.0",
    hasOlder: false,
    releases: [
    { version: "0.6.0", notes: "### 追加\n\n- **請求書以外の書式も開けるようになった**。検証シート・DB 設計書（RDB / NoSQL）・API 仕様書を、請求書や基本設計書と同じようにプレビューして A4 の PDF にできる。書式ごとに別の道具を探さなくてよい。\n- **見積書と領収書を、請求書と同じ書式で書けるようになった**。`種別` を書き分けるだけで表題・有効期限・但し書きが切り替わる。\n- **書き換えたものをそのままファイルへ保存できるようになった**（Ctrl+S で上書き、Ctrl+Shift+S で名前を付けて保存）。これまでは編集しても、元のファイルへ戻す手段が拡張の中に無かった。\n\n### 変更\n\n- frontmatter を読み取れなかったときの説明を、書いた人向けの言い方に変えた。これまでは英語の技術用語がそのまま出ていた。何行目の何がおかしいのか（字下げ・タブ・項目名の重複・閉じていない引用符など）を示す。\n\n### 修正\n\n- 依存ライブラリを脆弱性修正版へ更新した。" },
    { version: "0.5.0", notes: "### 追加\n\n- **Mermaid の図が描けるようになった**。0.4.0 では拡張の実行制限（MV3 の CSP）に触れるため見送っていたもので、必要な部分だけを取り出して組み直した。\n\n### 変更\n\n- 拡張の名前を「Markdown 業務文書ビューワー」にした。請求書だけでなく基本設計書も開けるようになり、以前の名前が中身と合わなくなっていたため。\n\n### 修正\n\n- 印刷ダイアログを閉じる（キャンセルする）とプレビューが消えてしまう問題を直した。" },
    { version: "0.4.0", notes: "### 追加\n\n- **基本設計書を開けるようになった**。請求書と同じように、プレビューと A4 の PDF 出力ができる。\n- **免税事業者の請求書のひな形を、ポップアップから始められるようになった**。ひな形の一覧は種類ごとに分けたので、目的のものを探しやすい。\n- 書き方ガイドに、発行元の書き方と免税事業者モードの説明を足した。\n\n### 修正\n\n- プレビューに流し込む前に内容を洗うようにした（DOMPurify）。読み込んだ Markdown に紛れ込んだ実行可能な記述が、そのまま画面で動くことがないようにするため。" },
    { version: "0.1.1", notes: "### 追加\n\n- **ひな形から書き始められるようになった**。ポップアップから選ぶと、その場で編集できる状態で開く。白紙から項目名を思い出す必要がない。\n- **左に原稿、右に仕上がりを並べて表示するようにした**。書きながら PDF の見た目を確かめられる。\n- **PDF の保存手順を画面で案内するようにした**。ブラウザの印刷ダイアログでヘッダーとフッターを切る手順まで含める。切らないと、日付と URL が紙に載る。\n- 日本語のフォント（Noto Sans JP / Noto Serif JP）を同梱した。環境によって字体が変わらない。\n- 請求書にテーマカラーとロゴを指定できるようにした。品目名の後ろに `|<数値>` を付けると、その行だけ文字の大きさを変えられる。\n\n### 変更\n\n- 入力の誤りを日本語で、どこが悪いのかが分かる形で出すようにした。\n- 拡張が求める権限を `storage` だけに絞った。\n\n### 修正\n\n- PDF が白紙になる／保存名が拡張の名前になってしまう問題を直した。\n- 請求書が 1 ページに収まらない問題を直した。" },
    { version: "0.1.0", notes: "### 追加\n\n- Markdown で書いた適格請求書をプレビューし、A4 の PDF として保存できる拡張の最初の版。" },
    ],
  },
  "workspace-addon": {
    version: "0.1.0",
    hasOlder: false,
    releases: [
    { version: "0.1.0", notes: "Google Workspace Marketplace への申請を準備している段階で、まだ配っていない。\n以下は `main` に入っている内容。\n\n### 追加\n\n- **検証シートを、スプレッドシートの画面そのままで書けるようになった**。Markdown を書ける人でなくても、いつも使っている表計算の操作で記入できる。列の構成と入力の制限（結果の選択肢など）は Markdown 側の定義から作られる。\n- **書いた内容を GitHub へ戻せるようになった**。サイドバーの「GitHub に push」を押すと、Markdown に組み直して commit する。どのリポジトリのどのファイルへ戻すかは、元の Markdown の frontmatter（`repository:`）に書いておく。\n  - **自動では送らない**。編集のたびに勝手に commit されると、書きかけの状態が履歴に残り続けるため、押したときだけ送る。\n- **手元の `.md` をサイドバーから読み込めるようになった**。GitHub に置いていないファイルも、貼り付けるか選ぶかで表に展開できる。\n- サイドバーに、いま何ができる状態なのかを出すようにした。接続に使う個人アクセストークンの有無、送り先のリポジトリ、記入済みの行数が分かる。トークンは伏せ字で表示する。\n\n### 変更\n\n- スプレッドシートを読む権限を、開いているファイルだけに絞った（`.currentonly`）。アカウント内の他のファイルは対象にならない。\n- 通信先を `api.github.com` に限る宣言をマニフェストへ入れた。\n\n### 修正\n\n- 表の空欄に `—` / `N/A` / `TBD` が入っていても、空欄として扱うようにした。埋めるための記号が値として書き出されると、後で本当に未記入の行を数えられなくなる。\n- Markdown へ書き出すときに frontmatter が消える問題と、日付が `Mon Jun 22 ...` の形で出てしまう問題を直した。\n- 表の末尾にある空行を書き出しに含めないようにした。\n- 依存ライブラリを脆弱性修正版へ更新した。" },
    ],
  },
};
