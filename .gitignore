# Dependencies
node_modules/
.pnpm-store/

# Build outputs
dist/
build/
.next/
.astro/
.turbo/
*.tsbuildinfo

# Release artifacts (Chrome Web Store uploads / GitHub release zips / signed crx)
release/
*.zip
*.crx

# Test
coverage/
.nyc_output/
# Playwright report / per-run artifacts (HTML reports, screenshots, traces)
test-results/
playwright-report/
dist-e2e-typecheck/

# IDE
.vscode/
.idea/
*.swp
*.swo

# OS
.DS_Store
Thumbs.db
desktop.ini

# Logs
*.log
npm-debug.log*
pnpm-debug.log*

# Env
.env
.env.local
.env.*.local

# Misc
.eslintcache
.cache/

# Changesets
# (.changeset itself is committed; pre-release status files are not)
.changeset/pre.json

# Per-company real data (must not land in OSS upstream)
# 各社の実データ（請求先名・登録番号・金額・印影など）は本家にコミットしない
/private/
/.local/
*.private.md
*.local.md
**/seal-*.png
**/seal-*.jpg
**/.kingdom/

# AI 開発者個人の作業ルール / 進捗ノート (OSS 上流に出さない)
# 司令塔リポからの配布元なので、本リポ (OSS 公開予定) には含めない方針。
# baseline / decisions / roadmap / project-status / issues / CLAUDE.md は
# クローンした開発者ごとに独自運用するため、tracked から外す。
/.claude/
/CLAUDE.md
