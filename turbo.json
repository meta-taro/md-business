{
  "$schema": "https://turbo.build/schema.json",
  "tasks": {
    "build": {
      "dependsOn": ["^build"],
      "outputs": ["dist/**", "build/**", ".next/**", ".astro/**"]
    },
    "bundle": {
      "dependsOn": ["^build"],
      "outputs": ["dist-sidecar/**"]
    },
    "dev": {
      "cache": false,
      "persistent": true
    },
    "lint": {
      "dependsOn": ["^build", "build"]
    },
    "typecheck": {
      "dependsOn": ["^build", "build"]
    },
    "test": {
      "dependsOn": ["^build", "build", "bundle"]
    },
    "test:run": {
      "dependsOn": ["^build", "build", "bundle"],
      "outputs": ["coverage/**"]
    }
  }
}
