# Project Status — md-business

最終更新: 2026-06-19（**v0.7.0 schema-test-spec Phase 3E TDD GREEN — google-workspace-addon サイドバーにアイコンパレット UX 完成**）: PdM フィードバック「textarea に YAML 直書きが繊細すぎる（インデント崩れ・古いサンプル混在で複数回失敗）」を受けて Phase 3E に着手。純粋関数モジュール `apps/google-workspace-addon/src/lib/frontmatterEdit.ts` 新規追加: `extractFrontmatter(src)` (frontmatter ⇔ body を `^---\r?\n([\s\S]*?)\r?\n---(?:\r?\n|$)/` で分離、CRLF 対応) / `replaceFrontmatter(src, newYaml)` (frontmatter が無ければ prepend / あれば置換) / `appendColumnToFrontmatter(src, column)` (js-yaml で load → columns 配列に push → dump、enum 時のみ values を保持、`name` 空白 / 未知 type は `{ok:false, error}`) / `removeLastColumnFromFrontmatter(src)` (末尾列を pop、frontmatter 無しならエラー) / `applyTestSpecTemplate(templateKey)` (minimal/full/clear 3 種。minimal=`schema: test-spec/v1`+documentNumber+title+issueDate+authors+1 列 / full=visualStyle 込み 7 列 (OK→緑文字 / NG→行赤 / 保留→セル黄) / clear=空文字)。`main.ts` に薄い trigger ラッパ 3 個 (`appendTestSpecColumn` / `removeLastTestSpecColumn` / `applyTestSpecTemplateAction`) を expose し、既存 `extractFrontmatterBlock` を `extractFrontmatter` 経由に集約 (重複実装解消)。`exactOptionalPropertyTypes: true` 衝突は conditional spread (`values` を持たせる/省くで分岐) で吸収。sidebar.html を全面改修: `<head>` に Google Fonts CDN で Material Symbols Outlined を読込 (IFRAME mode で許可)、テンプレ 3 アイコン (description/checklist/delete_sweep) + 列名入力欄 (`<input id="col-name">` 未入力時は連番「列N」自動命名) + 7 type アイコン (text_fields / notes / arrow_drop_down_circle / calendar_month / tag / check_box / link) + 末尾削除 (delete) + 既存 textarea/インポート・エクスポート/セットアップ・検証・md書き出し/PAT・自動同期 ON-OFF-状態 のボタン群を Material Symbols 32×32 pallet + 13px secondary ボタンで整理、enum クリック時のみ `prompt()` で values を CSV 受付 (空配列はエラー表示)、`setBusy()` / `setError()` で `cursor: progress` と `#137333` / `#c5221f` の status バナーを統一。`esbuild.config.mjs` の `TRIGGER_NAMES` に 3 関数追加。`js-yaml@^4.1.0` + `@types/js-yaml@^4.0.9` を addon の直接 dep に昇格 (phantom 排除、baseline §1)。テスト: `test/frontmatterEdit.test.ts` (17 件) で google-workspace-addon 全 **104 件緑** / frontmatterEdit.ts カバレッジ Stmts 96.66% Branch 90.24% Funcs 100% Lines 96.66% / 全体 98.57% / 91.47% / 100% / 98.57% (しきい値 80 全 4 軸クリア) / turbo 14 task 緑 / Code.js 199.9kb / Sidebar.html に `material-symbols-outlined` 12 出力。設計の要点: (1) **YAML 直書きを副入力として残しつつアイコンを主操作に**(textarea は編集も import も可、フォールバック経路を温存)。(2) 純粋関数 frontmatterEdit を分離して TDD カバレッジを集約、Apps Script trigger 層は薄いラッパに留める (testSpecSheetOps と同じ 2 段構造)。(3) サイドバー横幅は HtmlService 固定 ~300px (`setWidth` API 無し) を前提に、アイコン 32×32 + gap 6px で 7 個 + 削除 = 約 290px に収める設計。(4) enum の values は prompt() で CSV、Phase 3F で side-panel 入力欄に格上げ予定 (初版は最短経路)。(5) 既存 `extractFrontmatterBlock` の重複実装を削除し、唯一の真実源を frontmatterEdit に統一。Phase 3D 残り (Step 2-4: validateActiveTestSpecSheet / exportTestSpecMarkdown / installTestSpecAutoSync) は **PdM 手元の実シート + PAT 投入 (§15)** を要するため commit 後に並走で動作確認。commit `7122935` push 前 (人間 push / §6)。前ブロック: Phase 3C-3 TDD GREEN

2026-06-18 earlier（**v0.7.0 schema-test-spec Phase 3C-3 TDD GREEN — chrome-extension `test-spec` プラグイン完成**）: `apps/chrome-extension/src/plugins/test-spec.ts` を追加。`testSpecPlugin: SchemaPlugin<TestSpec>` (id `test-spec` / label `検証シート` / stylesHref `styles/test-spec.css`) を invoice/spec と完全に対称な構造で実装: `detect` (`列` / `列定義` / `検証項目列` / `columns` / `googleSheetId` / `sheetId` / `SheetId` / `シートID` / `連携シートID` のみを marker とし、`reviewers` / `レビュアー` は spec と共有なので意図的に除外) / `validate` (normalize → autofill → `validateWithCompiled` → `translateTestSpecErrors`) / `render` (markdownBody を core の `renderMarkdownToHtml` → `sanitizeViewerHtml` → `renderTestSpecBody(spec, {bodyHtml})`) / `previewRender` (`withPreviewDefaults` で必須欠落でも空のカバー + 列表を返す、autofill 警告と翻訳済みエラーを side channel として返す) / `documentTitle` (title 優先・空なら「検証シート ${番号}」) / `pdfFileName` (`renderTestSpecFileName`)。**`PluginRegistry.resolve` を拡張**: `schema` field の直接一致に加えて `schema: "test-spec/v1"` のような `<id>/<v>` 接頭辞も id 解決対象とする (test-spec は `schemaVersion` を持たず `schema` のみで運用する設計)。**`createDefaultRegistry` の登録順を `invoice → test-spec → spec` に変更**: spec.detect が broad (文書番号 / chapters / reviewers) なため、より特化的な test-spec を先に置いて detect 順を「より具体的なものが勝つ」原則に揃える。`apps/chrome-extension/package.json` に `@md-business/schema-test-spec: workspace:*` 追加。`scripts/post-build.mjs` の `copySchemaCss` に `test-spec.css` を追加、`STARTER_TEMPLATES` に `templates/test-spec/standard-ja.md` (category `検証シート`) を追加、`CATEGORY_ORDER` 末尾に `検証シート` 追加。テスト: `test/test-spec.test.ts` (19 件) + `test/registry.test.ts` に 4 件追加 (schema 接頭辞解決 / testSpec.detect / createDefaultRegistry 配列 / レビュアー単独では steal しない) で chrome-extension 全 **109 件緑** / test-spec.ts カバレッジ Stmts 95.45% / Branch 81.08% / Funcs 100% / Lines 95.45%、turbo `typecheck` + `test:run` + `build` 全 **21 task 緑**。設計の要点: (1) 既存 invoice/spec プラグインの構造を完全に踏襲 (将来の fork で挙動差異が出ないように)。(2) detect の marker 選定で spec と test-spec 共通の `reviewers` / `レビュアー` を意図的に除外、列定義 / シート連携 marker のみで判定。(3) 登録順を「より特化的なもの先」に揃えて detect 優先度を制御。(4) registry の `schema` 接頭辞解決は v0.7.0 で初めて必要になった分岐 (invoice は `schemaVersion: invoice/v1` + `schema: invoice` の二段だが test-spec は `schema: test-spec/v1` のみで運用)。(5) post-build.mjs のテンプレ追加で popup 「テンプレートから始める」に **検証シート** カテゴリが現れる → Phase 3D (PdM 動作確認) で「検証シート Markdown を開いて PDF 化」フローが完成。次は Phase 3D: PdM 動作確認 (Workspace Add-on 再 deploy → PAT 投入 → 検証シート編集 → 自動同期動作確認 + chrome-extension viewer での test-spec preview / PDF)。前ブロック: Phase 3C-2b TDD GREEN (commit `e93d1a7` push 完了)

2026-06-18 earlier（**v0.7.0 schema-test-spec Phase 3C-2b TDD GREEN — Apps Script onEdit installable trigger + UrlFetchApp 自動同期完成**）: `apps/google-workspace-addon/src/lib/autoSync.ts` に純粋関数 3 件追加: `prepareAutoSyncCommit({spec, sheetValues, sheetName, isoTimestamp, frontmatterBlock, customMessage?})` (extractRepoRefFromSpec → validateSheetValues → applySheetValuesToSpec → buildAutoCommitMessage を一本化、`skip(no_repository)` / `skip(validation_failed, validationIssues)` / `proceed(repoRef, markdown, commitMessage)` の 3 結果に整理) + `parseAutoSyncState(json | null): AutoSyncState` (`idle` / `pending` / `success` / `error` の discriminated union を fail-safe に復元、不正 JSON / 未知 kind は `idle` に collapse) + `serializeAutoSyncState`。Apps Script trigger 層 `src/main.ts` に副作用関数 8 件追加: `setGithubPat` / `clearGithubPat` / `hasGithubPat` (UserProperties に GitHub PAT を保存、**PdM 手作業投入のみ・AI 自走禁止 baseline §15**) + `installTestSpecAutoSync(markdownFrontmatter)` (frontmatter + frontmatterBlock + sheetName + repoRef を DocumentProperties に保存し `ScriptApp.newTrigger('handleTestSpecEdit').forSpreadsheet().onEdit()` で installable trigger 設置、idempotent に古い trigger 削除) + `uninstallTestSpecAutoSync` (trigger + Properties 全削除) + `handleTestSpecEdit(e)` (対象シート一致確認 + 既存 debounce trigger 削除 + 2 秒後の time-based trigger を作り直し + state = pending) + `flushPendingTestSpecSync` (Properties から状態復元 → prepareAutoSyncCommit → `UrlFetchApp.fetch` で GET 既存 sha → PUT Contents → success/error state 記録) + `getTestSpecAutoSyncStatus` (サイドバー表示用: installed/hasPat/sheetName/repoRef/state)。Sidebar.html に「PAT を保存 / 削除 / 自動同期 ON / OFF / 状態確認」5 ボタン + パスワード type の PAT 入力欄追加。`appsscript.json` の `oauthScopes` を更新: `spreadsheets.currentonly` → `spreadsheets` (installable trigger からのバックグラウンドアクセス必要) + `script.external_request` (UrlFetchApp) + `script.scriptapp` (Trigger 管理) の **3 scope 拡張 (Sensitive スコープ 3 件 — Phase E 検証申請が必要)**。`esbuild.config.mjs` の `TRIGGER_NAMES` に Phase 3B 漏れ 3 + Phase 3C-2b 8 = 11 関数を追加 (handleTestSpecEdit / flushPendingTestSpecSync を含めて Apps Script の文字列 trigger 解決に備える)。テスト: `test/autoSync.test.ts` (17 件) で google-workspace-addon 全 **87 件緑** / autoSync.ts カバレッジ Stmts 100% / Branch 96.96% / Funcs 100% / Lines 100%、turbo `typecheck` + `test:run` + `build` 全 21 task 緑 / Code.js 196.5kb。設計の要点: (1) `prepareAutoSyncCommit` を純粋関数化して trigger 層 (`flushPendingTestSpecSync`) は state 復元 + UrlFetchApp + state 保存だけに薄める (TDD カバレッジを純粋関数側に集約)。(2) debounce は time-based trigger の作り直しで実現 (`Utilities.sleep` だと 6 分実行上限を消費するため不可)。(3) AutoSyncState を discriminated union + サイドバーに「状態確認」ボタンで非同期 sync の状況を可視化 (PdM が手元で挙動確認できる)。(4) GitHub Contents API は GET → sha 取得 → PUT で「existing/new ファイル」両対応、404 は新規作成として継続、それ以外は error state。(5) Workspace Add-on で `spreadsheets.currentonly` → `spreadsheets` への scope 拡張は OAuth consent (Internal 運用) に追加同意が必要。次は Phase 3C-3: `apps/chrome-extension/src/plugins/test-spec.ts` でブラウザ側の test-spec 専用プレビュー (status バッジ + 列定義 + 検証表 + frontmatter `repository` リンク表示)。**PdM 確認ポイント (Phase 3C-2b)**: (a) appsscript.json scope 拡張のため Workspace Add-on は再 deploy → OAuth consent 同意し直し / (b) `PropertiesService.getUserProperties().setProperty('GITHUB_PAT', ...)` を Apps Script Editor または **新規サイドバー PAT 入力欄**で **PdM 自身が**投入 (AI は触らず baseline §15 厳守) / (c) GitHub PAT に `repo` (private) または `public_repo` (public) scope 必要。前ブロック: Phase 3C-2a TDD GREEN (commit `19b5564` push 完了)

2026-06-18 earlier（**v0.7.0 schema-test-spec Phase 3C-2a TDD GREEN — frontmatter `repository` field + GitHub binding 純粋関数完成**）: PdM 確定 = 案 A (frontmatter `repository` field) で GitHub binding を保存。`packages/schema-test-spec/` 拡張: (1) `src/test-spec.schema.json` に optional `repository: { pattern: "^[^/@:\\s]+/[^/@:\\s]+(@[^:\\s]+)?:[^\\s]+$" }` を追加 (parseRepoRef 互換 — owner/repo に `/@:` 空白禁止、`@branch` optional + スラッシュ可 + `:` 空白禁止、path 空白禁止)、(2) `src/types.ts` の `TestSpec` 型に `repository?: string` を追加、(3) `src/dictionary.ja.ts` root scope に「同期先リポジトリ / 同期先 / リポジトリ / リポ / repository / repo」を翻訳追加、(4) `dist/validate.compiled.js` 再生成。schema.test.ts 10 件 + normalize.test.ts 6 件追加で schema-test-spec 全 **129 件緑** / カバレッジ Stmts 99.64% / Branch 82.19% / Funcs 100% / Lines 99.64% (しきい値 80 全 4 軸クリア)。`apps/google-workspace-addon/src/lib/githubApi.ts` に純粋関数 2 件追加: `extractRepoRefFromSpec(spec): RepoRef | null` (spec.repository を parseRepoRef にかける、未指定 / malformed で null) + `buildAutoCommitMessage({spec, sheetName, isoTimestamp, customMessage?})` (デフォルト `chore(test-spec): sync ${documentNumber} from "${sheetName}" — ${iso}`、customMessage 優先、sheetName のダブルクォートはサニタイズ)。`@md-business/schema-test-spec` workspace dep を追加。9 件のテスト追加で google-workspace-addon 全 **70 件緑** / githubApi.ts カバレッジ Stmts 100% / Branch 89.65% / Funcs 100% / Lines 100%、turbo `typecheck` + `test:run` 全 14 task 緑。設計の要点: (1) **案 A 採用** は md で bind 先が可視化でき fork でも frontmatter ごと持っていける点を優先。(2) **pattern を parseRepoRef と一致** させて schema 段階で reject される入力は Apps Script 側に到達させない (エラー表示を 1 箇所に集約)。(3) `extractRepoRefFromSpec` / `buildAutoCommitMessage` を純粋関数で切り出し、Phase 3C-2b の Apps Script trigger 層 (`UrlFetchApp` / `PropertiesService` / `ScriptApp.newTrigger`) と分離。次は Phase 3C-2b: `main.ts` に `installTestSpecAutoSync` (frontmatter 保存 + installable onEdit trigger 作成) + `handleTestSpecEdit` (debounce: 2 秒後の time-based trigger を立て直す) + `flushPendingTestSpecSync` (validate → applySheetValuesToSpec → UrlFetchApp で Contents PUT)。PAT は `PropertiesService.getUserProperties().getProperty('GITHUB_PAT')` から取得、PdM 手作業投入 (baseline §15)。前ブロック: Phase 3C-1 TDD GREEN (commit `5f643f9` push 完了)

2026-06-18 earlier（**v0.7.0 schema-test-spec Phase 3C-1 TDD GREEN — GitHub API クライアント純粋関数完成**）: Phase 3C を 3 サブフェーズに細分化 (3C-1=純粋関数 / 3C-2=onEdit + UrlFetchApp / 3C-3=chrome-extension plugin) して baseline §7 + §20 順守。`apps/google-workspace-addon/src/lib/githubApi.ts` に `parseRepoRef(ref)` (`owner/repo@branch:path` 形式、`@branch` 省略時 `main` / branch スラッシュ対応 (`feat/test-spec`) / owner・repo・path 空で `null` / 前後空白 trim) + `buildContentsUrl(ref)` (GitHub Contents API URL、path はセグメントごと `encodeURIComponent` で `/` 保持、branch も `encodeURIComponent` で query encode) + `buildContentsPutPayload({message, contentBase64, branch, sha?, committer?})` (sha 省略時は key 自体を除外 / committer optional) を実装。テスト: `test/githubApi.test.ts` (14 件) で google-workspace-addon 全 **63 件緑** / githubApi.ts 単体カバレッジ Stmts 100% Branch 86.36% Funcs 100% Lines 100%、全体 98.97% / 89.85% / 100% / 98.97% (しきい値 80 全 4 軸クリア)、turbo 14 task 全緑。設計の要点: (1) Apps Script `Utilities.base64Encode` を純粋関数に持ち込まず、`contentBase64` を呼び出し側で受け取る引数化することで、Apps Script ランタイム依存をゼロに保ち test 環境で `Utilities` mock を要求しない。(2) Phase 3C-2 着手前に決める必要がある「GitHub binding (repo/branch/path) をどこから取るか」(frontmatter `repository` 追加 vs PropertiesService) の判断を本フェーズでは保留し、純粋関数 (文字列 ref パース + URL/payload 構築) のみに切り出すことで、後段でどちらに転んでも同じ関数群を再利用可能にする。(3) `parseRepoRef` の戦略は「最初の `/` で owner と残りを分離 → `@` 有無で branch/path 分岐 → branch にスラッシュ含む `feat/foo` 形式は最初の `:` で path と分離」で、`@main` 省略時はデフォルト `main`。次は Phase 3C-2: onEdit installable trigger + UrlFetchApp + PropertiesService PAT 経由の md 自動 commit (PAT 投入は baseline §15 により PdM 手作業)。**先に PdM 確認**: GitHub binding を frontmatter (`repository: "meta-taro/md-business@main:verify/login.md"` を `test-spec.schema.json` に optional 追加) で持つか、PropertiesService に保存するか。前ブロック: Phase 3B TDD GREEN (commit `f6ae6c5` push 完了)

2026-06-18 earlier（**v0.7.0 schema-test-spec Phase 3B TDD GREEN — Apps Script schemaToSheet / sheetToSchema / validateSheet 完成**）: `apps/google-workspace-addon/` に検証シート frontmatter ⇔ Sheets の双方向同期と D-1 確定設計のバリデーションを追加。純粋関数層 `src/lib/testSpecSheetOps.ts` = `planSheetWriteOps(spec)` (Sheets 書き込み計画の中間表現を返す: 列名 / FrozenRows=1 / DataValidation kind=list/date/number/checkbox/url / ConditionalFormat scope=row/cell + matchValue + 色) + `applySheetValuesToSpec(spec, values)` (Sheets 2D 配列 → md table body) + `validateSheetValues(spec, values)` (D-1 確定設計のバリデーション、`unknown_column` / `missing_column` / `enum_out_of_values` / `invalid_date` (ISO-8601 + Date オブジェクト両対応) / `invalid_number` / `number_out_of_range` / `invalid_checkbox` / `invalid_url` を 1-indexed row/col で返却)。Apps Script trigger 層 `src/main.ts` に `setupTestSpecSheet` / `validateActiveTestSpecSheet` (issue ごとに `setNote` 付与) / `exportTestSpecMarkdown` を追加。サイドバーに 3 ボタン追加 (セットアップ / 検証 / md 書き出し)。テスト: `test/testSpecSheetOps.test.ts` (36 件) で google-workspace-addon 全 **49 件緑** / カバレッジ Stmts 98.73% Branch 90.51% Funcs 100% Lines 98.73%（しきい値 80 全 4 軸クリア）/ esbuild bundle 180.1kb 成功。実装ポイント: (1) SpreadsheetApp API は test 環境で mock しづらいため、計画 (SheetWriteOps) → 適用 (Apps Script API) の 2 段階に分離。中間表現を純粋関数で生成し、テストはこれを検証、Apps Script trigger は薄く流すだけ。(2) D-1 確定設計を「**シート側の値は壊さない**」設計にして、`setNote` + サイドバー通知のみで検出を表現。(3) row_background は `=$<col>2="<value>"` の formula で行全体に適用、background/color はセル単位の TEXT_EQ 条件付き書式に分離。(4) parseTestSpec は `@md-business/schema-test-spec` の compiled validator (`validate.compiled.js` の default export) を DI で渡し、MV3 CSP 互換のままに保つ（Apps Script は CSP 不問だが、設計の対称性を維持）。次は Phase 3C: onEdit installable trigger + GitHub API client (PropertiesService に PAT を PdM 手作業投入、baseline §15) + chrome-extension `plugins/test-spec.ts` への組み込み。前ブロック: Phase 3A TDD GREEN (commit `2bf3cc2` push 完了)

2026-06-18 earlier（**v0.7.0 schema-test-spec Phase 3A TDD GREEN — renderer-pdf testSpecTemplate + template fixture 完成**）: `packages/renderer-pdf/` に検証シート向けの A4 縦テンプレを実装。`src/testSpecTemplate.ts` (renderTestSpecBody) + `src/renderTestSpecHtml.ts` (full HTML wrapper) + `src/styles/test-spec.css` (cover + 列定義テーブル + 検証表 body スタイル、`spec.css` の準拠) + `templates/test-spec/standard-ja.md` (日本語 frontmatter サンプル: ログイン機能 7 列構成 + visual NG=行赤 / 保留=セル黄 / OK=文字緑 + 検証行 6 件)。テスト: `test/testSpecFixtures.ts` + `test/testSpecTemplate.test.ts` (27 件) + `test/renderTestSpecHtml.test.ts` (9 件) で 36 件追加 → renderer-pdf 全 **148 件緑** / カバレッジ Stmts 98.38% Branch 89.87% Funcs 100% Lines 98.38%（しきい値 80 全 4 軸クリア）。実装ポイント: (1) 表紙 = ステータスバッジ 4 種 (draft/review/executing/completed) + meta dl + 作成者/レビュアー/関連文書/Google Sheets ID の各セクション、未設定時は省略。(2) 列定義セクション = column の name / type ローカライズ (text→文字列, multiline_text→複数行, enum→プルダウン, date→日付, number→数値, checkbox→チェックボックス, url→URL) + enum 値の `(A / B / C)` 表記 + number range の `[min 〜 max]` 表記 + visual 書式は `<ul>` で enum キーごとに「行背景色 / 背景色 / 文字色」 をラベル付き表示。(3) Theme color は spec/invoice と同じ preset 7 色 + hex 受付、CSS injection を `HEX_COLOR` で拒否。(4) `RenderTestSpecBodyOptions.bodyHtml` で本文 HTML 注入、`hideCover` で表紙抑制。(5) schema は `'test-spec/v1'` で `data-schema-version` 属性に escape 出力。renderer-pdf 依存に `@md-business/schema-test-spec` workspace 追加 + `exports./styles/test-spec.css` 追加。次は Phase 3B: Apps Script `schemaToSheet` / `sheetToSchema` / `validateSheet` (D-1 確定設計: detect+notify irregular cells)、Phase 3C: onEdit + GitHub API + chrome-extension plugin への組み込み。前ブロック: Phase 2 TDD GREEN (commit `71c3745` + `4e728e3` push 完了)

2026-06-18 earlier（**v0.7.0 schema-test-spec Phase 2 TDD GREEN — translateError + fileName 完成**）: Phase 1 (74) に 39 件のテスト追加 → 全 **113 件緑** / カバレッジ Stmts 99.63% Branch 82.19% Funcs 100% Lines 99.63%（しきい値 80 全 4 軸クリア）。実装の中身: (1) `src/translateError.ts` = test-spec 固有 path × keyword の日本語化。**`X` placeholder** を導入し `column.visual/<enum 値>/row_background` の `<enum 値>`（OK/NG/保留 等）をエラーラベルに動的反映（`normalizePath` で visual 直下の非数字キーを `X` に折り畳み、`applyIndices` で `{X}` を埋める）。`PATTERN_HINTS` = SemVer + hex 色 3 種 / `FORMAT_HINTS` = `/issueDate` / `ALLOWED_VALUES` = status + column.type 7 種 / `additionalProperties` は親 label 直接表示。(2) `src/fileName.ts` = token 展開（`{文書番号}/{documentNumber}` 他 14 種）+ Windows 禁止文字 → `_` サニタイズ。**デフォルトテンプレ = `検証シート_{文書番号}_v{版}`**（schema-spec の「基本設計書_…」と並ぶ命名）。(3) PdM から「nosql 向け DB 設計書テンプレあれば優先度低で導入したい」依頼を受け、Phase 2 chain は破断せず TaskCreate #40 + roadmap v0.8.0+ 行に追記（commit 後に調査着手）。次は Phase 3: `templates/test-spec/standard-ja.md` + Apps Script `schemaToSheet/sheetToSchema/validateSheet`（D-1 確定設計）+ chrome-extension plugin + renderer-pdf テンプレ。前ブロック: Phase 1 TDD GREEN (commit `b382bd7` push 完了)

2026-06-18 earlier（**v0.7.0 schema-test-spec Phase 1 TDD GREEN — schema/types/normalize/autofill/parseTestSpec 完成**）: `packages/schema-test-spec/` 雛形を完成し失敗テスト先行 → 4 テストファイル / 74 件全緑 / カバレッジ Stmts 100% Branch 94.87% Funcs 100% Lines 100%（しきい値 80 全 4 軸クリア）。設計合意 → 雛形 → ビルド成功 → テスト緑のチェーンを 1 セッションで完走。実装の中身: (1) `src/test-spec.schema.json` = `schema: const "test-spec/v1"` / `columns: minItems: 1` 必須 / column 7 種 (text/multiline_text/enum/date/number/checkbox/url) / `if type=enum then required: values` 条件付き必須 / `visualStyle.row_background|background|color` のみ許容（hex pattern） / `additionalProperties: false` を全層強制 / `googleSheetId` は optional（onEdit ON 時に runtime required を別レイヤで強制予定）。(2) `src/types.ts` = `TestSpec` 型 + `ColumnType` union + `ColumnVisualStyle` + `TestSpecPerson` + status union (`draft|review|executing|completed`)。(3) `src/dictionary.ja.ts` = 4 scope (`root|party|column|visualStyle`) 辞書 + `STATUS_TRANSLATIONS` / `COLUMN_TYPE_TRANSLATIONS` / `THEME_VALUE_TRANSLATIONS`。(4) `src/normalize.ts` = `translateLeaf` に **`column.visual` 特別扱い**（dict-of-visualStyle 構造: 外側キー = enum 値そのまま、内側 = visualStyle scope で再帰翻訳）を追加（初版で `childScopeFor` だけでは外側 dict キーが翻訳対象になってしまうバグを 1 サイクルで発見・修正）。(5) `src/autofill.ts` = defaults (`schema/version/status`) + 列名重複警告 + enum without values 警告 + visual キーが values にない警告（authoring mistake を早期通知）。(6) `src/parseTestSpec.ts` = `splitFrontmatter → normalize → autofill → validateWithCompiled` パイプライン、`validate` は DI で MV3 CSP 互換維持。(7) `scripts/build-validate.mjs` = schema-spec と同じ require→`import * as` 巻き上げで `ReferenceError: require is not defined` 防止。次は Phase 2: `translateError.ts`（日本語化バリデーションエラー）+ `fileName.ts`（{documentNumber} token 展開）の TDD。前ブロック: v0.7.0 設計合意確定 (commit `ed1a0f3` push 完了)

2026-06-18 earlier（**v0.7.0 schema-test-spec 設計合意完了 — PdM 戦略判断 3 件確定**）: PdM 回答: **D-1** = イレギュラーセル/未知列は「捨てる / 取り込む」の二択ではなく **検出して通知**（サイドバー警告 + `setNote` + 行背景色）/ **E-1** = onEdit 自動同期を **v0.7.0 入り**（Sheets 編集 → debounce 2 秒 → validation → 通過行のみ GitHub API で md commit。Phase E 検証申請所要は Sensitive スコープ 3 個で 3〜4 週 → **5〜7 週** に延長を許容）/ **F-1** = パッケージ名 `schema-test-spec` 確定（音声入力 typo「敗訴の名前」を文脈解釈、違ったら次ターン訂正）。残論点 A-1/A-2/A-3/B/C-1/C-2/C-3 は AI が推奨案で確定（`schema: test-spec/v1` ヘッダ / `columns` 必須化 / `googleSheetId` を同期 ON 時必須に昇格 / 初版型 7 種 / visual キー 2 種 / ConditionalFormatRule 実装方針 / frontmatter 真で常に上書き）。`appsscript.json` の `oauthScopes` に `script.external_request`（Sensitive）+ `script.scriptapp` を追加予定、GitHub PAT は `PropertiesService.getUserProperties` に PdM 手作業投入（baseline §15）。conflict resolution は v0.7.0 では最終編集勝ち、3-way merge は v0.8.0 以降。次セッションで TDD 着手（`packages/schema-test-spec/` の失敗テスト先行 → schemaToSheet / sheetToSchema / validateSheet → onEdit + GitHub API クライアントは最後に）。前ブロック: v0.7.0 設計合意フェーズ起票 (commit `716c31c`)

2026-06-18 earlier（**v0.7.0 schema-test-spec 設計合意フェーズ着手**）: PdM 発端（「プルダウンとか、NGなら行を薄い赤背景にするとか可能ですかね？」）が schema-test-spec の本質を直接表現していたため、合意フェーズの論点を `.claude/issues/002-v0.7.0-schema-test-spec.md` に展開。8 件の判断項目をチェックリスト化。前ブロック: Phase C-1/C-2 完遂 + submit guide 実走知見反映 (commit `f798d88`)

2026-06-18 earlier（**Phase C-1/C-2 完遂 + submit guide を実走知見で大幅改訂**）: テストデプロイ → Sheets サイドバー → md ⇔ シート双方向 import/export を PdM 手元で実機確証（B2 を `OK` → `NG` に編集してエクスポートし textarea に Markdown 反映されることを目視確認）。Phase B-1〜B-3 → A-4 → C の順序制約、Workspace ドメインポリシーの External + Testing preempt 挙動、Side Panel アイコンが `addOns.common.logoUrl` 404 で非表示になる挙動、`useLocaleFromApp: true` の暗黙 `script.locale` 要求挙動、個人 Gmail（`ladygo755@gmail.com`）からの「アクセス権限をリクエスト」誤動作と baseline §17 violation 寸前事案（業務 Gmail 側で「不承認」処理 + Internal 切替で恒久対処）、4 アカウント同時ログインによる context flip の根本対処（Chrome プロファイル分離）を `docs/google-addon-submit-guide.md` に折り込み。`script.container.ui` を Sensitive scope と訂正（Phase E 検証申請の所要時間見積もりに影響）。`apps/google-workspace-addon/appsscript.json` を最終構成（`useLocaleFromApp` 削除 / `logoUrl` = `https://www.gstatic.com/images/branding/product/2x/apps_script_48dp.png` の Google CDN placeholder）に確定。OAuth User type は「内部に公開」運用（Phase F 直前に「外部」に戻す）。次は schema-test-spec v0.7.0 で frontmatter スキーマ駆動のデータ検証 / 条件付き書式（OK/NG プルダウン / NG 行薄赤背景 / 日付ピッカー / ヘッダー固定）→ Marketplace submit。前ブロック: clasp v3 .claspignore セマンティクス fix

2026-06-18 同日 earlier（**Phase 2 Google Workspace アドオン雛形を双方向化 + 申請素材を仕上げ**）: (1) Sheets → md エクスポート機能追加（`valuesToMdTable` 実装 + `exportActiveSheetToMarkdown` Apps Script trigger 追加 + サイドバーに「エクスポート」ボタン追加 = md ⇔ Sheets 双方向の片方向 fallback が成立。(2) `docs/google-addon-marketplace-listing.md` で Marketplace listing 草案（短文 80 字 / 詳細約 3000 字 / カテゴリ / タグ / アイコン仕様 / スクショ仕様 / FAQ）を PdM が Console にコピペで貼れる形で documentation 化。(3) `PRIVACY.md` を md-business シリーズ全体のポリシーに拡張し、Workspace アドオン向けの `.currentonly` スコープ宣言 + 外部送信なし宣言 + 将来 GitHub 連携時の追加スコープ予告を追記。(4) `apps/google-workspace-addon/examples/test-spec-sample.md` 追加（Phase C 動作確認用の検証シートサンプル、frontmatter `googleSheetId` 想定）。13 tests pass / カバレッジ 100% / monorepo 12 tasks green。前ブロック: 雛形 + 申請手順書 commit b4cba1e

## 現在のフェーズ

**Phase 1-MVP 公開済み（v0.1.0）→ v0.5.0 を Web Store update として submit 済（審査待ち）+ Phase 2 Google Workspace アドオン土台作り着手（2026-06-18）**

2026-06-16 にドコカデ Inc. アカウントで v0.1.0 を submit、2026-06-17 に審査通過＆公開。store URL: https://chromewebstore.google.com/detail/lmdplkkfmgapnhombimeohjliinifgjh （extension ID 固定）。

v0.2.0 〜 v0.5.0 のスコープを 1 つの zip にまとめて v0.5.0 として提出する（バージョン 0.1.1 → 0.5.0 への単発 bump）:
- v0.2.0 系: DOMPurify サニタイザ + schema-spec（基本設計書）骨格 + remark→HTML 変換
- v0.3.0 系: 免税事業者モード（`taxExemptIssuer` 後方互換拡張、renderer-pdf が経過措置案内を自動出力、tax-exempt-ja テンプレ）
- v0.4.0 系: popup の適格 / 免税分岐 UX + 書き方ガイドモーダルに免税モード解説 + frontmatter 早見表更新
- v0.5.0 系: Mermaid 動的 import 描画復活（cytoscape 空 shim + lodash CSP 違反 vite plugin 置換、ER / flowchart / sequence など対応）+ 基本設計書 spec.css に章タイトル / Mermaid 図の改頁制御を追加（6.1 注文確定フロー regression 対応、`max-height: 190mm` 確定で 15p → 13p に圧縮、章タイトル + フローチャート全体が p10 同居）

Mermaid の MV3 CSP 突破は **2026-06-17 朝の撤回（id 61）を即日撤回（id 67）**: cytoscape を `src/shims/cytoscape-empty.ts`（call で throw する空 shim）に alias で差し替え、`vite.config.ts` の `shimLodashCspViolations()` plugin で lodash 由来の `Function('return this')()` を `globalThis` に、`x.require('util').types` を `undefined` に置換。`mermaid.initialize({ securityLevel: 'strict' })`、class marker による short-circuit で請求書フローは zero-cost、scan-bundle clean。

Issue #15（高橋たくと氏 → 株式会社キングダム宛 6 月分請求書）が v0.3.0 のドライバ、すでに schema/renderer/template/private 全レイヤで成立済み。

## 完了

- 2026-06-12 GitHub リポ `meta-taro/md-business` 作成（public + MIT）
- 2026-06-12 Phase 0 骨格焼き込み（pnpm workspace + LICENSE + CI + CLAUDE.md + baseline）
- 2026-06-12 Phase 0 初期 Issue 起票
- 2026-06-14 Phase 1-MVP スコープ確定（請求書 + Chrome 拡張のみ）
- 2026-06-14 Chrome 拡張表示名確定: `md-business: Invoice (適格請求書)`
- 2026-06-14 印影なし方針確定（法的義務なし、CSS で署名欄余白のみ）
- 2026-06-14 各社実データ `.gitignore` 排除運用確定（`/private/` `*.private.md` 等）
- 2026-06-14 Chrome 拡張をスキーマプラグイン構造にし、後の schema 追加で継続アップデート方針
- 2026-06-15 `packages/schema-invoice` 適格請求書 v1 スキーマ実装（#3）
- 2026-06-15 `packages/renderer-pdf` 請求書 HTML レンダラ実装（#5、32 tests、Paged.js 連携）
- 2026-06-15 `apps/chrome-extension` MVP 実装（#8、MV3 + popup + viewer + content script + SchemaPlugin）
- 2026-06-15 MV3 CSP 違反の根治: gray-matter → js-yaml 置換、Ajv standalone code-gen 導入、`@md-business/core` を browser-safe / `./runtime` に分割
- 2026-06-15 baseline 項目5 補完: Husky 9 + `.husky/pre-commit`（lint/typecheck/test:run）+ `.husky/pre-push`（build + bundle 走査）+ Vite `EVAL` warning の error 化
- 2026-06-15 Ajv standalone の `require()` 残置（ucs2length / ajv-formats）を build-validate.mjs の後処理で top-level `import * as` に持ち上げ、ブラウザの `ReferenceError: require is not defined` を根治。scan-bundle に `require("...")` 検出（文字列/コメント除外つき）も追加
- 2026-06-16 PDF DL ガイドモーダル実装（A 方針: Chrome 印刷ダイアログ自動起動 + 「送信先=PDFとして保存」案内 + `mdb:pdf-guide-skip` localStorage フラグでスキップ可）
- 2026-06-16 Noto Sans JP / Noto Serif JP を `@fontsource/*` 経由で 9 ファイル同梱。`scripts/post-build.mjs` で `dist/vendor/fonts/` にコピー + `dist/styles/fonts.css` 自動生成。viewer 起動時に `chrome.runtime.getURL` で動的読み込み。`MdBusiness Body` / `MdBusiness Stamp` alias を追加し fork での篆書差し替えに対応
- 2026-06-16 篆書系 OSS フォント調査: 青柳衡山フォント T はカスタムライセンスで再配布不可、JFZSKSealScript は OFL だが日本語非対応 → Noto Serif JP Bold で代替、Phase 1.1 で再評価
- 2026-06-16 ハンコ SVG ジェネレータ実装（`packages/renderer-pdf/src/stamp.ts`）。`stamp.shape: auto` は法人キーワード（株式会社/有限会社/合同会社/合名会社/合資会社 / `(株)` / `㈱` 系）で角印、それ以外は丸印を自動判定。1/2/3/4 字レイアウト分岐、ASCII は横書き 1 行、CJK は縦書き / 田の字。朱色 `#c8161d`、フォントは `MdBusiness Stamp` alias 経由
- 2026-06-16 schema-invoice に `stamp?: { enabled?, shape?, text?, font? }` フィールド追加。Ajv standalone 再ビルド、20 件のハンコテスト追加（renderer-pdf 全 52 tests pass、stamp.ts 98% カバレッジ）
- 2026-06-16 schema-invoice v0.2: 日本語 frontmatter キー（請求書番号/発行日/発行元/請求先/品目/振込先/印影/丸め/ファイル名）対応、normalize+autofill 2 段、autofill で taxSummary/totals を items[] から自動計算（既定丸め floor、適格請求書 B2B 慣行）。`templates/invoice/standard-ja.md` 追加。`schema-invoice` 全 45 tests pass、branch 84.48%
- 2026-06-16 PDF 保存ファイル名のテンプレート指定対応（`fileName: "御請求書_{請求先}{敬称}_{発行元}_{YMD}"` のような token 展開、Windows 禁止文字をサニタイズ）。viewer は `window.print()` 直前に `document.title` を差し替え、終了後復元
- 2026-06-16 バリデーションエラーの日本語化・可視化。`packages/schema-invoice/src/translateError.ts` で path × keyword を日本語ラベル（「請求先の名前は必須項目です」「発行元の登録番号: T で始まる 13 桁の数字…」等）に翻訳。Chrome 拡張 viewer に warning バナー（黄）/ error 構造化リスト（赤）を追加。`@md-business/core` の `ValidationResult` に warnings を任意プロパティとして拡張。`schema-invoice` 全 69 tests + chrome-extension 全 14 tests pass
- 2026-06-16 請求書レイアウト圧縮 + 空行パディング。CSS の余白 / フォントを 15〜25% 圧縮、`renderInvoiceBody` に `minItemRows`（既定 5）を追加し品目が少ない場合は空行で埋める。3 項目で 2 ページ目に溢れていた問題を解消。renderer-pdf 全 57 tests pass
- 2026-06-16 viewer を split-pane 化（左 CodeMirror 6 エディタ / 右ライブプレビュー iframe）。`previewMarkdown()` を新設し validation 失敗時も `withPreviewDefaults` で構造を補って描画継続、エラーは右パネルバッジ + 入力中はブロックしない。PDF DL ボタン押下時のみ strict 検証 → 失敗ならエラーバナー、合格なら iframe で Paged.js を読み込んで `iframe.contentWindow.print()`。Paged.js を iframe に隔離してツールバー / モーダルを破壊しない構造。chrome-extension 全 27 tests pass、branch カバレッジ 84.33%
- 2026-06-16 frontmatter に `theme`（青/赤/黄/橙/紫/黒/灰 のプリセット or `#RRGGBB`）と `logo`（`data:image/{png,jpeg,gif,webp};base64,...` or `https://...`）対応。CSS 変数 `--mdb-color-accent` を `.mdb-invoice` の inline style で上書き、ロゴは発行元当事者ブロック上部に `max-height:14mm` で表示。テーマ色は `resolveThemeColor` で preset/hex のみ受け付け、ロゴは regex 厳格ホワイトリスト（XSS / CSS injection 防御）
- 2026-06-16 印影で姓名間の空白を除去する fix（`extractStampChars` で `\s+` 除去）。「山田 太郎」が `['山','田',' ','太']` になり 4 文字目「郎」が落ちていた事象を修正。renderer-pdf 全 77 tests pass、stamp.ts 98% カバレッジ
- 2026-06-16 Chrome 拡張 release zip 生成スクリプト（`apps/chrome-extension/scripts/make-release.mjs`）。Pure Node zlib + 自前 CRC32 + ZIP local header / central directory / EOCD 実装。source-map / OS junk / `.zip` / `.crx` を除外、`dist/` の中身を archive root に展開。`pnpm release` で `release/md-business-v<version>.zip` を出力
- 2026-06-16 Web Store 用スクリーンショット 3 枚を 1280x800 / 24bit PNG (アルファなし) に letterbox 変換（Pillow、横 1280 fit + 上下白帯、Lanczos resampling）。`release/screenshots/webstore-1280x800-{1,2,3}.png`
- 2026-06-16 Chrome 拡張 manifest 権限を実使用に合わせて最小化（`storage` のみ）。`activeTab` / `scripting` は実コードで未使用のため削除、申請フォームの理由欄削減 + 最小権限原則準拠
- 2026-06-16 `PRIVACY.md` 追加（ゼロデータ収集宣言、`chrome.storage.session` 使用範囲明記、リモートコード不使用宣言）。Web Store の Privacy Policy URL は GitHub blob 直リンクを登録
- 2026-06-16 Chrome Web Store 申請完了（v0.1.0、ドコカデ Inc. アカウント、後で公開モード）。審査結果待ち
- 2026-06-16 サンプル md（`private/sample-contractor-invoice.md`、業務委託エンジニア → ドコカデ宛、4 品目 ¥616,000、テーマ青、丸印）を Web Store スクリーンショット用に生成
- 2026-06-16 v0.1.1 patch 実装完了・push 済（`343f5e3` `5f7c725`）。popup「テンプレートから始める」3 ボタン（standard-ja / standard / inbound-eligible）+ `dist/templates/` 同梱 + `scripts/bump-version.mjs`（厳格 SemVer + lib/version.ts 分離で vitest 透過）+ `manifest.json`/`package.json` 同期 bump + `chrome.runtime.getURL()` + `web_accessible_resources` 追加。chrome-extension 36 tests pass（うち bumpVersion 9 件）、217 tests 継続
- 2026-06-16 `apps/chrome-extension/README.md` に Chrome Web Store 審査者向けサンプル md の GitHub Raw URL 表（standard-ja / standard / inbound-eligible）を追記
- 2026-06-16 `feat/v0.2.0` ブランチ作成（main 温存）。`packages/schema-invoice` / `plugins/invoice.ts` / `renderer-pdf/src/template.ts` の invoice 系・`manifest.json` の version を触らない方針を Issue 001 に明記
- 2026-06-16 v0.2.0 設計合意 Issue 起票（`.claude/issues/001-v0.2.0-schema-spec.md`）。並行 Explore で SDD テンプレ事例（MADR / arc42 / Google Design Docs）+ Mermaid/SVG/MV3 ライブラリ選定を調査、Issue に反映
- 2026-06-16 v0.2.0 B 案（章ツリー）を **B1 単一 md + B3 chapters: 明示参照** 併用で確定。A 案（ファイル名 prefix 順序制御）は人間負担で不採用、API 仕様書（v0.4.0+）も同じ C 哲学（インデックス + 明示参照）で行く方針確定
- 2026-06-17 Issue #12（baseline 1 違反疑い）調査・解決。`pnpm-workspace.yaml` の `onlyBuiltDependencies: []` / `minimumReleaseAge: 1440` は正常動作（`pendingBuilds: []` + install ログ無 postinstall）。`pnpm config get` は npm 互換 global config のみ読む仕様で workspace settings は見ないため未定義は誤検知。CI に install ログ grep 検証ステップを追加し将来の退行を検出（c53de03）
- 2026-06-17 `templates/spec/standard-ja.md` 追加（EC 注文管理サブシステム基本設計書、8 章 / Mermaid 図 4 種 / 表 49 行 / A4 横 6 ページ想定 / 日本語 frontmatter A 案）。v0.2.0 設計合意の数ページ評価用サンプル（16410e7）
- 2026-06-17 v0.5.0 Mermaid 復活実装。`mermaid@11.15.0` 動的 import + `<pre><code class="language-mermaid">` short-circuit、`vite.config.ts` で cytoscape 系 3 パッケージを `src/shims/cytoscape-empty.ts` に alias、`shimLodashCspViolations()` vite plugin で lodash CSP 違反パターン（`Function('return this')()` / `x.require('util').types`）を browser-safe 実装に置換（lodash-es + `@mermaid-js/parser` dist の両方）。`apps/chrome-extension/src/shared/renderMermaid.ts` 87 行 + 4 unit tests、`packages/renderer-pdf/src/styles/spec.css` に `.mdb-mermaid` / `.mdb-mermaid--error` 追加、`viewer/index.ts` の `renderPreviewFromSource` と `runPrintFlow` を await async 化。`scripts/scan-bundle.mjs` clean、chrome-extension 36 tests pass、`v0.4.0 → v0.5.0` minor bump（`scripts/bump-version.mjs`）
- 2026-06-17 `packages/schema-spec/` 骨格実装。JSON Schema draft 2020-12（required: schemaVersion/documentNumber/title/version/issueDate/status/authors、SemVer pattern、ISO date format、`.md` chapters pattern、additionalProperties: false）+ TypeScript 型 + 日本語キー dictionary（root + party scope、status/toc/theme 値翻訳）+ Ajv standalone build。44 tests pass（schema 14 + normalize 30）、coverage 100% lines / 94.87% branches
- 2026-06-17 `packages/schema-spec` 完成度向上: autofill（schemaVersion / version=0.1.0 / status=draft / toc=auto デフォルト、toc=manual で chapters 空時の警告、toc=auto と chapters 同時指定の mixed signal 警告）+ translateError（Ajv エラー / normalize / autofill 警告を日本語化、SemVer / `.md` / YYYY-MM-DD ヒント、enum 許可値を明記）+ parseSpec（splitFrontmatter → normalize → autofill → validateWithCompiled の MV3 CSP セーフな End-to-End）+ fileName（`{文書番号} / {タイトル} / {版} / {ステータス} / {発行日YMD} / {YMD}` token、Windows 禁止文字サニタイズ、デフォルト `基本設計書_{文書番号}_v{版}`）。96 tests pass（schema 14 + normalize 30 + autofill 10 + translateError 21 + fileName 14 + parseSpec 7）、coverage 99.52% lines / 82.57% branches
- 2026-06-17 `packages/renderer-pdf` に基本設計書レイアウト追加。`renderSpecBody(spec, options)` で表紙ページ（タイトル / ステータスバッジ / 文書番号 / 版 / 発行日 / 作成者 / レビュアー / 関連文書、テーマアクセントカラー、左端 6mm カラーバー）+ `renderSpecHtml(spec, options)` フル HTML 文書ラッパ + `src/styles/spec.css`（A4 縦、表紙 page-break-after、h1 章ごと page-break-before、h2 左ボーダー、テーブル / コードブロック / Mermaid コンテナ / 引用 / 画像スタイル）。`bodyHtml` は viewer 側責務（chrome-extension の plugins/spec で md→HTML 変換予定）。CSS injection 攻撃 / XSS のテストカバー含む 29 tests pass（specTemplate 20 + renderSpecHtml 9）、renderer-pdf 全 106 tests pass、specTemplate / renderSpecHtml 共に 100% カバレッジ
- 2026-06-17 `@md-business/core` に `renderMarkdownToHtml(src, options)` 追加。`remark-parse → remark-rehype → rehype-stringify` の unified パイプライン（純 JS、MV3 CSP セーフ）で Markdown → HTML 変換。`allowDangerousHtml: false` で生 HTML を破棄（XSS 防御）、`hasFrontmatter` オプションで frontmatter 分離有無を制御。19 tests、100% カバレッジ
- 2026-06-17 `apps/chrome-extension` に `shared/sanitizeHtml.ts` 追加（DOMPurify 3 ベース）。Markdown→HTML 後の bodyHtml を viewer で injection 防御。`USE_PROFILES: { html, svg, svgFilters }` で SVG カメルケース属性（viewBox/preserveAspectRatio）を保持、`ALLOW_UNKNOWN_PROTOCOLS: true` + `uponSanitizeAttribute` フックで URI スキーム allowlist（https / blob / mailto / `data:image/{png|jpg|gif|webp|svg+xml};base64,` + 相対 `# / ? / /`、`http:` / `data:text/html` / `javascript:` / `vbscript:` / 相対パスは拒否）。`FORBID_TAGS` で form/input/iframe/object/embed をブロック、`FORBID_ATTR` で srcdoc 除去。spec plugin の render/previewRender からサニタイザを呼ぶよう更新。テスト環境を node → jsdom に切替（happy-dom は HTML 先頭 `<h1>` を head に正規化し本文先頭の章タイトルが消える既知挙動のため不採用）。24 件のサニタイザテスト追加（XSS / 属性ホワイトリスト / SVG / Mermaid コード塊保持 / 通常 markdown 通過）、chrome-extension 全 77 tests pass、sanitizeHtml.ts 92.42% カバレッジ
- 2026-06-17 `apps/chrome-extension` に `plugins/spec.ts` 追加。`SchemaPlugin` 契約を `render(frontmatter, markdownBody?)` / `previewRender(frontmatter, markdownBody?)` の 2 引数に拡張（invoice plugin は body 無視、spec plugin は使用）。`loadMarkdown` / `previewMarkdown` を `parseMarkdown().body` も渡すよう更新。registry に spec plugin を登録（detect: 文書番号 / 章ファイル / レビュアー / documentNumber / chapters / reviewers）。post-build.mjs を `copySchemaCss`（invoice.css + spec.css 両方）に統合 + `templates/spec/standard-ja.md` を STARTER_TEMPLATES に追加（popup から「テンプレートから始める」で開けるように）。chrome-extension 全 53 tests pass（spec plugin 14 + registry 14 + invoice loadMarkdown 8 + previewMarkdown 8 + bumpVersion 9）、spec.ts 96.2% カバレッジ。repo 全体で typecheck + test:run + build green
- 2026-06-17 **v0.3.0 免税事業者モード** schema-invoice 改修。`invoice.schema.json` の `issuer.required` を `["name"]` のみに縮約、`issuer.taxExemptIssuer: boolean` プロパティ追加（後方互換）。`InvoiceIssuer.registrationNumber` を optional 化 + `taxExemptIssuer?: boolean` 追加。`dictionary.ja.ts` party scope に `免税事業者` / `taxExemptIssuer` / `免税` / `非適格` / `インボイス未登録` → `taxExemptIssuer` 正本マッピング追加。`autofill.ts` に `checkIssuerQualification` 追加（both-set / neither-set を警告通知、path: `issuer.taxExemptIssuer` / `issuer.registrationNumber`）。`translateError.ts` で `/issuer/taxExemptIssuer` 日本語ラベル追加 + `registrationNumber` ヒントに「免税事業者なら taxExemptIssuer: true で代替可」を追記。schema-invoice 全 81 tests pass（既存テストの後方互換性維持 + 免税モード 7 tests 追加）
- 2026-06-17 **v0.3.0 renderer-pdf 免税事業者表示分岐**。`renderInvoiceBody` で `invoice.issuer.taxExemptIssuer === true` を検知し、タイトル `<h1>請求書</h1>` 直下に `<p class="mdb-invoice__non-qualified-notice">※ 適格請求書ではありません（インボイス制度経過措置の対象）</p>`（赤字 #b91c1c、9pt、600 weight）と、本文末尾の備考 section 直前に `<section class="mdb-invoice__transition-notice">本請求書は適格請求書発行事業者以外が発行したものです。インボイス制度の経過措置（2023年10月〜2029年9月）の範囲で仕入税額控除を行ってください。</section>`（グレーボックス・左端アクセントボーダー）を自動出力。`.mdb-invoice` ルート要素に `data-tax-exempt="true"` 属性も付与してテーマ別 CSS 拡張可能。`styles/invoice.css` に対応スタイル追加。renderer-pdf 全 112 tests pass（免税事業者モード 6 tests 追加、`taxExemptInvoice()` fixture factory）
- 2026-06-17 **v0.3.0 chrome-extension invoicePlugin / popup 調整**。`plugins/invoice.ts` の label を `請求書（適格請求書）` → `請求書（適格 / 免税対応）` に更新。`apps/chrome-extension/scripts/post-build.mjs` の `STARTER_TEMPLATES` に `tax-exempt-ja.md` 第 4 エントリを追加（label: `日本語フィールド名・免税事業者向け`、description: 免税事業者 + 経過措置案内自動出力）。`dist/templates/manifest.json` に 5 件のテンプレが揃った
- 2026-06-17 **v0.3.0 テンプレ `templates/invoice/tax-exempt-ja.md`** 追加。屋号なし個人事業主想定（山田 太郎 → 株式会社サンプル受領先宛）、90,000 × 10% = 9,000 / total 99,000 サンプル。`免税事業者: true` を明示、taxSummary / totals は YAML コメントで参考値（feedback-invoice-manual-calc 方針）を残して autofill に委譲、登録番号フィールドは存在しない。日本語キー throughout、テーマ青、丸印スタンプ無効
- 2026-06-17 **v0.3.0 private/2026-06-contractor-invoice.md** を高橋たくと氏の本番テンプレに整備。旧 `登録番号: ""` + TODO コメント → `免税事業者: true` に切替、備考から redundant な経過措置記述を削除（renderer が自動出力する）。3 リポジトリ運用（store-girlsbar / store-concafe-maid / store-cabaret）× 30,000 円/月 = 90,000 + 税 9,000 = 99,000 円。ファイル名: `御請求書_{請求先}{敬称}_高橋たくと_{YMD}`
- 2026-06-17 v0.3.0 完了時点で **repo 全体 408 tests pass / lint 10 pkg green / typecheck 10 pkg green / 5 pkg build green**。dist/templates/manifest.json に 5 テンプレ揃った（standard-ja / standard / inbound-eligible / **tax-exempt-ja** / spec/standard-ja）
- 2026-06-17 **v0.4.0 検収フィードバック対応（6 件まとめて 1 コミット）**: (1) renderer-pdf 免税テンプレからタイトル直下の朱書き「※ 適格請求書ではありません」と「〜へ」受領者サブタイトルを撤回（商習慣配慮、本文末尾の経過措置案内は維持）。(2) `@md-business/core` の `renderMarkdownToHtml` に `remark-gfm@^4.0.0` 追加、`templates/spec/standard-ja.md` の「3.1 機能一覧」pipe table が `<table>` として描画されるよう修正。(3) `packages/renderer-pdf/src/styles/spec.css` の表紙を A4 1 ページ収まりに圧縮: `@page :first` margin: 0、`.mdb-spec__cover` を `min/max-height: 297mm` + `overflow: hidden`、title 28→26pt + 余白 18→12mm、meta 14→10mm、status 14→10mm、cover-inner padding 40/24/32/22 → 24/22/14/22mm に圧縮。(4) `@media screen` の `box-shadow` と外側 8mm margin を撤去（プレビューと印刷の見た目を一致）。(5) **印刷キャンセル時のプレビュー消失バグ修正**（apps/chrome-extension/src/viewer/index.ts `runPrintFlow`）: プレビュー iframe を上書きする旧実装を撤回し、専用の隠し iframe（`#mdb-print-frame`）を毎回作って印刷後に `remove()` する方式へ。キャンセル時もメイン UI は無変化。core 44 tests + renderer-pdf 112 tests + chrome-extension 77 tests + 全体 472 tests pass、scan-bundle clean、v0.4.0 zip 再生成済み
- 2026-06-18 **Phase 2 Google Workspace アドオン雛形** `apps/google-workspace-addon/` 新規作成。clasp + esbuild + TypeScript + vitest + @vitest/coverage-v8 + @types/google-apps-script + @types/node の workspace 構成。`esbuild.config.mjs` で IIFE バンドル + globalName=mdb + footer に Apps Script trigger wrapper（onHomepage / onOpen / onInstall / showSidebar / getSidebarHtml）を inject。`appsscript.json` で oauth scope を `.currentonly` 系 + `script.container.ui` の **非センシティブのみ** に限定（OAuth 検証申請をスキップ可能な構成）、addOns.common.homepageTrigger + docs/sheets/slides 各 homepageTrigger 宣言。`src/main.ts` に CardService ベースの homepage card（「サイドバーを開く」ボタン）+ Editor Add-on 互換の onOpen / onInstall + `importMarkdownTableToActiveSheet`（Markdown table 文字列を現在の Sheet に書き込む先行スパイク）。`src/sidebar.html` に vanilla HTML サイドバー（textarea + ボタン + google.script.run 連携）。`src/lib/mdTable.ts` に薄い Markdown table パーサ（schema-test-spec 完成時に core 統合判断）。`.claspignore` で dist/ のみ push、`.gitignore` で `.clasp.json` / dist / coverage を排除。6 unit tests pass / カバレッジ 100%、build で `dist/Code.js`（4.0kb）+ `Sidebar.html` + `appsscript.json` 生成、monorepo turbo 全 12 tasks green。`docs/google-addon-submit-guide.md` に PdM 手作業手順（Phase A〜F: GCP プロジェクト / OAuth consent / アドオン動作確認 / Marketplace listing / 検証申請 / 公開申請）を時系列で documentation 化、baseline 15 に従い AI 補佐範囲と PdM のみ実行範囲を明確分離。実 Marketplace submit は `schema-test-spec` (v0.7.0) 完成と同時 = 本日は土台作りまで（[`.claude/decisions.md`](.claude/decisions.md) 2026-06-18 行）
- 2026-06-18 **Phase 2 アドオンを双方向化 + 申請素材整備** (commit 後続)。(1) `src/lib/mdTable.ts` に `valuesToMdTable`（2D array → md table 文字列、空セル正規化 + `|` エスケープ + 改行→空白置換）追加、`src/main.ts` に `exportActiveSheetToMarkdown` trigger 追加、`esbuild.config.mjs` の `TRIGGER_NAMES` に新 trigger 2 件追加。サイドバー HTML に「インポート」「エクスポート」2 ボタンレイアウト + 共通 setBusy/setError 関数追加。テストは valuesToMdTable に 7 ケース追加（基本 / 空 / header 単独 / pipe escape / 改行置換 / 型強制 / parse↔values の round-trip）= 13 tests / カバレッジ 100%。(2) `docs/google-addon-marketplace-listing.md` 新規作成（アプリ名 30 字 / 短い説明 80 字 / 詳細 4000 字以内 / カテゴリ / タグ / アイコン仕様 / スクショ仕様 / YouTube 動画指針 / FAQ 草案 / 提出前チェックリスト）。Marketplace SDK の Console に PdM がコピペで貼れる形に整理、baseline 15 に従い「貼り付けは PdM」を明示。(3) `PRIVACY.md` を md-business シリーズ全体のポリシーに拡張、Chrome 拡張パート保持しつつ Workspace アドオン向けに「現在開いているドキュメントのみ」スコープ説明 + 外部送信なし宣言 + 将来 GitHub 連携時の追加スコープ予告を追記、改訂履歴セクション追加。(4) `apps/google-workspace-addon/examples/test-spec-sample.md` 追加（frontmatter `googleSheetId` 想定の検証シートサンプル、Phase C 動作確認で「貼って試す」用）。monorepo turbo 12 tasks green
- 2026-06-18 **Phase 2 Phase C-1/C-2 完遂**: PdM 手元で `clasp push --force` → Apps Script Editor で「デプロイをテスト → Sheets / Docs / Slides にインストール」→ Sheets を新規作成して右側 Side Panel に md-business アイコン表示 → サイドバー起動 → md table 貼り付け → 「インポート」で A1:C2 に書き込み (項目/OK/NG/備考 + ログイン成功/OK/-) → B2 を `OK` → `NG` に編集 → 「エクスポート」で textarea に `| ログイン成功 | NG | - |` 反映、を実機確証。途中、(a) `addOns.common.logoUrl` に存在しない GitHub raw URL を指定していて Side Panel アイコンが消えていた事象 → Google CDN の Apps Script 公式ロゴに差替で復旧、(b) `useLocaleFromApp: true` の暗黙 `script.locale` 要求でランタイムエラー → `useLocaleFromApp` 行削除で復旧、(c) Workspace ドメインポリシーが External + Testing 状態のテストユーザー登録を preempt して OAuth が「アクセス権限をリクエスト」モードになり個人 Gmail から共有依頼メールが業務 Gmail へ → 業務 Gmail 側で「不承認」+ OAuth User type を「内部に公開」に切替で恒久対処（baseline §17 violation 寸前）、(d) Chrome に 4 アカウント同時ログイン → 他 3 つを一時ログアウトで context flip 解消（恒久対処は dokokade.co.jp 専用 Chrome プロファイル分離 = 別 Issue で扱う）。これらの実走知見を `docs/google-addon-submit-guide.md` に折り込み: タイムライン表に A-4 を Phase B 後ろに位置変更 + 「実施順序の注意」節新設 + A-2 API リストに Sheets/Docs/Slides API 推奨追加 + B-2 補足 `logoUrl` 必須 + B-3 で `script.container.ui` を Sensitive 訂正 + `useLocaleFromApp` 警告 + B-4 補足「内部に公開」ルート + 「アクセス権限をリクエスト」罠 + Phase C 詳細手順 + 詰まりポイント表 + トラブルシュート 5 行追加。`.claude/decisions.md` に 2026-06-18 行を 7 本追記

## 進行中

- v0.5.0 Chrome Web Store update 提出済（審査待ち）
- Phase 2 Google Workspace アドオン Phase C-1/C-2 完遂（2026-06-18）→ Marketplace submit は schema-test-spec (v0.7.0) 完成と同時
- **v0.7.0 schema-test-spec 設計合意完了**（PdM 戦略判断 3 件確定、実装未着手）: D-1 = 検出+通知 / E-1 = onEdit 自動同期も v0.7.0 入り / F-1 = `schema-test-spec` 確定。次セッションで TDD 着手（`packages/schema-test-spec/` の失敗テスト先行）
- PdM 推奨タスク（未着手・優先低）: dokokade.co.jp 業務専用 Chrome プロファイル分離

## 次タスク

### v0.4.0 提出パッケージ生成（直近）

1. ✅ `manifest.json` / `package.json` を 0.1.1 → 0.4.0 へ bump
2. ✅ roadmap / decisions / project-status を Mermaid 延期 + v0.4.0 統合に更新
3. ✅ `pnpm --filter @md-business/chrome-extension release` で `release/md-business-v0.4.0.zip` 再生成（Mermaid 抜き）
4. ✅ scan-bundle clean（6 bundled JS, 0 violations）
5. ✅ PdM 検収フィードバック（6 件: 朱書き撤回 / 経過措置案内維持 / GFM pipe table / 表紙 1 ページ / シャドウ撤去 / 印刷キャンセルバグ）を反映 → zip 再々生成
6. ⏳ PdM が push → PdM が Web Store update 提出

### v0.5.0 minor（請求書実務細部 + Mermaid 代替）

1. frontmatter `振込手数料: ご負担 | 元負担` を schema-invoice に追加（autofill が備考末尾に文章生成、上書き可能）
2. frontmatter `締日基準: 月末 | 20日 | カスタム` を追加（renderer-pdf が支払期限の文章補足を生成）
3. frontmatter `丸め: 切り捨て | 四捨五入 | 切り上げ` の UI 説明・選択肢のドキュメント整備（既に内部は `taxRounding` 実装済）
4. viewer に「適格事業者登録番号の調べ方」リンクモーダル（国税庁の公表サイト案内）
5. `templates/invoice/` に振込手数料込み・締日カスタムのサンプル
6. **Mermaid 代替手段** — spec テンプレの ` ```mermaid ` ブロックは現状生ソース表示。代替案を比較検討（A: 静的 SVG 埋め込み運用へ寄せる / B: 軽量代替ライブラリ調査 / C: mermaid + 必要 chunk のみ vendor 化して CSP 適合させる）
7. **manifest.json / package.json の version を 0.5.0 に bump → release zip → PdM が Web Store update 提出**

## Chrome 拡張の拡張性方針（SemVer 運用）

Chrome 拡張は単発リリースで終わらず、md-business シリーズ全体の受け皿として継続アップデートする。バージョンは SemVer に従い、0.x のうちは MINOR で破壊変更余地を残し、schema API 安定後に v1.0 を打つ。

- スキーマレジストリ型: 拡張内で `schema-invoice` `schema-spec` `schema-test-spec` 等を切替可能
- ビューワー / レンダラもスキーマごとにプラグインとして差し替え可能
- 新スキーマ追加時は MINOR bump で Chrome Web Store に再 submit、patch bump は同スキーマ内の改善

| 拡張バージョン | リリース時期 | 対応スキーマ / 主機能 |
|---|---|---|
| v0.1.0 | 2026-06-16（公開済み） | `invoice`（請求書）MVP |
| v0.1.x | 2026-06-16〜 | invoice patch（テンプレ始動 UX、ファイル名 UI、書き方ガイド拡充 等） |
| v0.4.0 | 2026-06-17 AM（zip 提出待ち） | + `schema-spec`（基本設計書）+ 画像 / SVG / DOMPurify サニタイザ + 免税事業者モード + popup 適格/免税分岐 UX（Mermaid は CSP 違反で延期） |
| v0.5.0 | 2026-06-17 PM（zip 提出待ち） | + Mermaid subset build 復活 + 免税事業者 1 ページ収まり再発 fix + Playwright E2E（renderer-pdf）|
| v0.5.x+ | 2026-06-18〜 | + 請求書実務細部（振込手数料 / 締日 / 端数処理）|
| v0.6.0+ | Phase 3 以降 | + `test-spec` / `quotation` / `meeting-minutes` / `contract` / `resume` 等 |
| v1.0.0 | schema API 安定 + 主要スキーマ揃った時点 | 安定版宣言 |

## 将来構想（未決定・Phase 1b 以降で検討）

- **共同編集**: 現状はローカル md 単独編集だが、複数人同時編集を視野に入れる。技術候補は Yjs / Automerge / CRDT。Phase 1b 以降の PWA レイヤに組み込むのが現実的。
- **動作モード**: ローカル単独（Phase 1-MVP）→ クラウド同期（git / Dropbox 経由・Phase 1b）→ リアルタイム共同編集（Phase 2 以降）の段階的展開。
- **ブランチ戦略**: Phase 1-MVP は `main` で走り切る（PdM 指示 2026-06-14）。請求書 md 完成後に Issue 化して決定。候補: Phase 単位 feature ブランチ + PR / Git Flow（main + develop）/ 各社カスタムは fork ベース（decisions.md 既存方針延長）。

## 既知の懸念

- npm scope `@md-business/` の空き未確証 → Phase 3 npm 公開直前に `npm view @md-business/probe` で実機確認
- LINE LIFF の業務文書系 OSS 先行事例なし → Phase 4 で完全新規実装
- Chrome Web Store 審査期間（初回 1〜2 週）→ 6 月分は最悪 PdM 代行でカバー

## 関連

- Roadmap: [./roadmap.md](./roadmap.md)
- Decisions: [./decisions.md](./decisions.md)
- baseline: [./rules/product-baseline.md](./rules/product-baseline.md)
