# Issue 002: v0.7.0 検証シートスキーマ（schema-test-spec）+ Sheets double-binding

- **Status**: 設計合意フェーズ（実装未着手）
- **Branch**: `feat/v0.2.0` を継続（または `feat/v0.7.0` に切り直しを PdM 判断）
- **Target release**: Chrome 拡張 v0.7.0 + Workspace アドオン v0.2.0（**Marketplace submit と同タイミング**）
- **Roadmap**: Phase 2（Workspace アドオン submit ゲート）

## 目的

業務文書シリーズの 3 番目のスキーマとして「検証シート（Test Spec / 検収チェックリスト）」を追加し、**Markdown frontmatter で列スキーマを宣言 → Google Sheets に DataValidation / ConditionalFormat / DateValidation / setFrozenRows を自動適用する double-binding** を実装する。これにより `schema-test-spec` の完成と同時に Google Workspace アドオンを Marketplace submit する（v0.7.0 は Phase 2 submit のゲート）。

本 Issue は実装着手前の **設計合意フェーズ**。

## 背景・前提

- v0.6.0（File System Access API + 請求書実務細部）が roadmap 上に存在するが、v0.7.0 は **v0.6.0 を待たずに並行で進めて良い**（schema-test-spec パッケージは invoice / spec と独立、Workspace アドオン側の dist も独立）
- 2026-06-18 Phase 2 Phase C-1/C-2 完遂で、`md ⇔ Sheets` の **裸の table 双方向 import/export** は確証済（commit `f798d88`）。本 Issue はそこに **frontmatter スキーマ駆動の double-binding を載せる** タスク
- 2026-06-18 decisions: 「Google アドオンの主軸は md ⇔ Sheets 双方向 UI 編集」「Sheets-as-truth 方式」「Marketplace submit は schema-test-spec 完成と同時」が確定済
- PdM の発端的な気づき（2026-06-18 14:30 セッション末尾）: 「プルダウンとか、NGなら行を薄い赤背景にするとか可能ですかね？」→ Apps Script で技術的に完全可能。frontmatter スキーマで宣言する設計が schema-test-spec の本質

## デグレ防止

`feat/v0.2.0` 作業中は以下に **触らない**:

- `packages/schema-invoice/` 配下すべて
- `packages/schema-spec/` 配下すべて（spec パッケージは v0.5.0 で完成済）
- `apps/chrome-extension/src/plugins/invoice.ts` / `apps/chrome-extension/src/plugins/spec.ts`
- `packages/renderer-pdf/src/template.ts` / `specTemplate.ts` の invoice / spec 専用ロジック
- `apps/google-workspace-addon/src/lib/mdTable.ts` の `parseMdTable` / `valuesToMdTable`（v0.1.0 の裸 table 互換性、test-spec は別関数で実装）
- 既存テスト（2026-06-18 時点で全 472 件）を 1 件も削除しない

## 設計合意ポイント（PdM 判断必要）

### A. frontmatter スキーマ構造案

```yaml
---
schema: test-spec/v1
documentNumber: TEST-2026-001
title: ログイン機能 検証シート
version: 0.1.0
issueDate: "2026-06-18"
status: executing       # draft | review | executing | completed
authors:
  - { name: 田中 正智, role: 検証担当 }
reviewers:
  - { name: 山田 太郎, role: PM }
relatedDocs:
  - ./../spec/login-system.md       # 基本設計書（schema-spec v0.5.0）への参照
googleSheetId: "1abc...xyz"         # 連携先 Sheets ID（双方向同期で使用、未設定時は import/export のみ）
columns:
  - { name: ID,         type: text }
  - { name: 検証項目,    type: text }
  - name: 期待結果
    type: multiline_text
  - name: 結果
    type: enum
    values: [OK, NG, 保留, 未実施]
    visual:
      NG:   { row_background: "#FFE5E5" }
      OK:   { background: "#E5F5E5" }
      保留: { background: "#FFF5D5" }
      未実施: { background: "#F0F0F0", color: "#999" }
  - name: 担当
    type: text
  - name: 実施日
    type: date
  - name: 備考
    type: multiline_text
---

| ID    | 検証項目                          | 期待結果              | 結果   | 担当 | 実施日     | 備考       |
|-------|-----------------------------------|----------------------|--------|------|------------|------------|
| T-001 | 正しいパスワードでログイン成功      | ホーム画面へ遷移      | OK     | 田中 | 2026-06-18 |            |
| T-002 | 誤ったパスワードでエラー表示        | エラーメッセージ表示  | NG     | 田中 | 2026-06-18 | 想定外成功 |
| T-003 | 存在しない ID でエラー表示          | ユーザ未登録メッセージ | 未実施 |      |            |            |
```

**合意したい点**:

- (A-1) `schema: test-spec/v1` ヘッダで良いか（既存パッケージは `schemaVersion: spec/v1` だが PdM 議論 2026-06-16 では `schema: ...` 形式が AI 親和性高いとの示唆）
- (A-2) `columns` を必須にして「列スキーマ未宣言」を warning にする、または columns 省略時は本文 table のヘッダ行から自動推測（type=text 統一）する fallback を持つか
- (A-3) `googleSheetId` の役割: 未設定時は手動 import/export のみ、設定時は onEdit 自動同期（v0.7.0 では未実装、v0.7.1 以降）の予約フィールドとしておく方針で良いか

### B. 列の `type` 列挙

初期は **7 種**:

| type | Sheets での実装 | Markdown 側 |
|---|---|---|
| `text` | DataValidation なし | 単純セル |
| `multiline_text` | DataValidation なし、改行可 | export 時に `<br>` → `\n` 変換 |
| `enum` | `requireValueInList(values).setAllowInvalid(false)` | values 配列を frontmatter 宣言 |
| `date` | `requireDate()` | ISO-8601 (`YYYY-MM-DD`) |
| `number` | `requireNumberBetween(min, max)`（min/max 任意） | 数値リテラル |
| `checkbox` | `insertCheckboxes()` | `[x]` / `[ ]` |
| `url` | DataValidation なし（v0.7.1 で URL 検証検討） | 単純テキスト |

**合意したい点**: 上記 7 種で v0.7.0 の十分性を確認。他に `boolean` / `time` / `currency` / `percentage` を初版から入れるべきか、後発でも問題ないか。

### C. 視覚スタイル（`visual:`）の記法

```yaml
- name: 結果
  type: enum
  values: [OK, NG, 保留, 未実施]
  visual:
    NG:   { row_background: "#FFE5E5" }   # 行全体の背景色
    OK:   { background: "#E5F5E5" }       # セル単独の背景色
    保留: { background: "#FFF5D5", color: "#996600" }  # 文字色も指定可
```

**合意したい点**:

- (C-1) `row_background`（行全体）と `background`（セル単独）の 2 キーで区別する設計で良いか
- (C-2) Apps Script の `setConditionalFormatRules` 実装: 行全体 = `whenFormulaSatisfied('=$結果列="NG"')` + 全列レンジ / セル単独 = `whenTextEqualTo('OK')` + 該当列のみ。これで正しい
- (C-3) Markdown export 時に `visual:` を完全保持して書き戻す（= ラウンドトリップ可能）。シート側で書式が手動編集された場合は frontmatter が真として再 import 時に上書き

### D. 双方向同期の責務分離（最重要）

| 何が真か | 編集権 | export → import で復元されるか |
|---|---|---|
| **列スキーマ（`columns`）** | Markdown frontmatter が真 | ✅ |
| **視覚スタイル（`visual`）** | Markdown frontmatter が真 | ✅ |
| **データ検証ルール（DataValidation）** | Markdown frontmatter が真 | ✅ |
| **行データ（テスト項目 / 結果 / 担当 / 実施日 / 備考）** | **Sheets が真** | ✅ |
| **シート側で追加された未知の列** | ❓ | ❓ → **PdM 合意必要** |

**合意したい点（D-1）**: シート側で勝手に追加された列の扱い

- 案 X: **Markdown 優先** — export 時に未知の列は捨てる（シート側で実験的に追加したものは保持されない）。実装シンプル、frontmatter が常に真
- 案 Y: **Sheets 取り込み** — export 時に未知の列を `columns` に `type: text` で追記して frontmatter に書き戻す。柔軟だが「frontmatter が真」の原則と矛盾
- 推奨: **案 X**（schema-test-spec のコンセプトは「列スキーマは Markdown で設計」なので、シート側はあくまで「データ入力面」）

### E. v0.7.0 のスコープ境界

| 機能 | v0.7.0 | v0.7.1 以降 |
|---|---|---|
| frontmatter `columns` → DataValidation / ConditionalFormat / DateValidation 自動適用（import 時） | ✅ | |
| Sheets → md export 時に `columns` + `visual` を frontmatter として書き戻し（手動「エクスポート」ボタン） | ✅ | |
| ヘッダー行固定 + 列幅自動調整 | ✅ | |
| Chrome 拡張側のプレビュー（schema-test-spec plugin） | ✅ | |
| PDF 出力（A4 縦テーブル形式） | ✅ | |
| **onEdit 自動同期（Sheets 編集 → GitHub API → md commit）** | ❌ | **v0.7.1** |
| Marketplace submit | ✅ (Phase D-F 並行) | |

**合意したい点（E-1）**: onEdit 自動同期を v0.7.0 に含めるか、v0.7.1 に後送りするか
- 推奨: **v0.7.1 に後送り**。理由: (a) onEdit は GitHub OAuth scope 追加が必要で OAuth 検証申請（Phase E）の所要時間が増える、(b) 手動 import/export ボタンだけでも検証シート運用は成立する、(c) submit と onEdit を分離することで Marketplace 公開を早められる

### F. パッケージ名・モジュール構成

- `packages/schema-test-spec/`（新規）
  - `src/schema.ts` — JSON Schema（Ajv standalone code-gen）
  - `src/normalize.ts` — 日本語キー → 正本キー（`結果` → `result` 等の synonym 吸収）
  - `src/autofill.ts` — `schema: test-spec/v1` / `status: draft` / `issueDate` 既定値
  - `src/translateError.ts` — Ajv エラー日本語化
  - `src/parseTestSpec.ts` — split → normalize → autofill → validate
  - `src/fileName.ts` — `{documentNumber}_{title}_{YMD}` token
  - `src/dictionary.ja.ts` — 列名 / 型 / status / visual キーの synonym
  - `test/*.test.ts` — TDD 失敗テスト先行

- `apps/google-workspace-addon/src/lib/`
  - `schemaToSheet.ts` — frontmatter `columns` → `DataValidation[]` + `ConditionalFormatRule[]` + `setFrozenRows(1)` + 行データ書き込み
  - `sheetToSchema.ts` — Sheets `getDataRange()` + `getDataValidations()` + `getConditionalFormatRules()` → frontmatter + body md table
  - 既存 `mdTable.ts` の `parseMdTable` / `valuesToMdTable` は **そのまま温存**（v0.1.0 の裸 table 互換）

- `apps/chrome-extension/src/plugins/test-spec.ts`（新規）— 検証シートプレビュー
- `packages/renderer-pdf/src/testSpecTemplate.ts`（新規）— A4 縦テーブル PDF

**合意したい点（F-1）**: パッケージ名 `schema-test-spec` で確定で良いか
- 他案: `schema-test-cases` / `schema-verification` / `schema-checklist` / `schema-qa`
- 推奨: `schema-test-spec`（既存の `schema-invoice` / `schema-spec` と命名揃え、roadmap 既載）

## 完了条件（v0.7.0 リリース時）

- [ ] `packages/schema-test-spec/` が `pnpm test:run` で 100% pass、coverage 95% 以上
- [ ] `apps/google-workspace-addon/` の import/export ボタンで `schema: test-spec/v1` frontmatter を含む md を貼ると、Sheets に列スキーマ準拠で DataValidation / ConditionalFormat が適用される
- [ ] 同 Sheets を編集して export ボタン → textarea に frontmatter + 行データが round-trip で復元される
- [ ] `apps/chrome-extension/src/plugins/test-spec.ts` 経由で `templates/test-spec/standard-ja.md` がプレビュー可能
- [ ] `packages/renderer-pdf/` で A4 縦の検証シート PDF を出力
- [ ] `templates/test-spec/standard-ja.md` をサンプルとして同梱、Chrome 拡張 popup の「テンプレートから始める」第 5 ボタンに追加
- [ ] `docs/google-addon-submit-guide.md` Phase D-F を実走可能な状態にまで詰める
- [ ] 全テスト（既存 + 新規）pass、scan-bundle clean
- [ ] Phase D: Marketplace listing 提出 → Phase E: 検証申請（`script.container.ui` Sensitive の用途文書 + デモ動画）→ Phase F: 公開申請

## 作業ログ

### 2026-06-18 PdM 合意（commit `<追記>` で記録）

PdM 回答（要約）:

- **D-1**: 「エラー、イレギュラーなセルを通知したいです」→ **第 3 案を採用**: 捨てる/取り込むの二択ではなく、**イレギュラーを検出して通知する** 設計（後述「D-1 確定設計」）
- **E-1**: 「早めにほしいです。そうするとシート修正時にエラーが同期されて案内してくれますよね」→ **onEdit 自動同期を v0.7.0 に含める**。GitHub OAuth scope 追加 + Phase E 検証申請の所要日数延長を許容
- **F-1**: 「（スキーマ）の名前でいいです」→ **`schema-test-spec` で確定**（音声入力 typo 想定、違ったら次ターンで訂正）

残論点（A-1 / A-2 / A-3 / B / C-1 / C-2 / C-3）は AI 判断で **推奨案を確定** として進める。違いがあれば次ターンで指摘してください:

- **A-1**: `schema: test-spec/v1` ヘッダで進める
- **A-2**: `columns` を **必須**にする（自動推測 fallback は持たない、明示宣言の徹底で AI 親和性を確保）
- **A-3**: `googleSheetId` は「**onEdit 同期 ON のとき必須**、未設定時は手動 import/export のみ」の運用フィールドへ昇格（E-1 が v0.7.0 入りしたため、もはや「予約」ではない）
- **B**: 初版型 **7 種**（text / multiline_text / enum / date / number / checkbox / url）で進める
- **C-1**: visual キーは `row_background`（行全体）/ `background`（セル単独）の 2 キーで進める
- **C-2**: 実装方針確定（行全体 = `whenFormulaSatisfied('=$N$2="NG"')` + 全列レンジ / セル単独 = `whenTextEqualTo('OK')` + 該当列）
- **C-3**: ラウンドトリップ時の手動編集上書きは **frontmatter 真の原則** で常に上書き（手動編集された書式は再 import で消える、書式を残したい場合は frontmatter `visual:` を編集する）

### D-1 確定設計（イレギュラー通知）

Markdown 優先で捨てる/Sheets 取り込みの二択ではなく、**Markdown スキーマと Sheets 実データの差分を検出して通知する**。

| 検出対象 | 通知方法 | 復旧 |
|---|---|---|
| シート側に未知列が追加された | サイドバーに警告 + 該当列ヘッダにメモ（`SpreadsheetApp.Range.setNote`） | PdM が「Markdown に列追加」or「シート列を削除」を選択 |
| `enum` 値が `values` 外（例: `OK/NG/保留/未実施` の枠を超えた `?`） | 当該セルに薄黄色 + 警告メモ + サイドバーに件数表示 | PdM がセル修正 or `values` を frontmatter で拡張 |
| `date` 型に非日付（手動入力ミス） | 当該セルに薄赤 + 警告メモ + サイドバーに件数表示 | PdM がセル修正 |
| `number` 型に文字列 | 当該セルに薄赤 + 警告メモ + サイドバーに件数表示 | PdM がセル修正 |
| `checkbox` 型に boolean 以外 | （DataValidation で防ぐので発生しないはず、防御として検出） | – |
| 必須列の空セル（将来 `required: true` 対応時） | 当該セルに薄赤 + サイドバーに件数表示 | PdM が入力 |

実装方針:

- `apps/google-workspace-addon/src/lib/validateSheet.ts`（新規）
  - 入力: Sheets `getValues()` + frontmatter `columns`
  - 出力: `ValidationResult[] = { row, col, columnName, type: 'unknown_column' | 'enum_out_of_values' | 'invalid_date' | ..., currentValue, expected, message }[]`
- import 時（md → Sheets）: validation 実行不要（Markdown が真なので常に整合）
- export 時（Sheets → md）: validation 実行 → 結果をサイドバーに表示 → `markdownBody` に追加するか否かは PdM 操作（OK ボタンで export 強行 / キャンセル）
- onEdit 自動同期時: validation 実行 → 結果をシートに **メモ（`setNote`）** + サイドバーに「⚠ N 件のイレギュラー」表示 → GitHub commit はバリデーション通過行のみ反映（イレギュラー行は手元に通知のみで commit 保留）

### E-1 確定設計（onEdit 自動同期）

#### スコープ追加

`apps/google-workspace-addon/appsscript.json` の `oauthScopes` に追加:

```json
"https://www.googleapis.com/auth/script.external_request",
"https://www.googleapis.com/auth/script.scriptapp"
```

- `script.external_request`: **Sensitive**（`UrlFetchApp` で GitHub API を叩く）
- `script.scriptapp`: 非 Sensitive（`ScriptApp.newTrigger` で installable trigger を作成）

Phase E 検証申請（OAuth verification）の所要日数: 3〜4 週 → **5〜7 週**（Sensitive スコープが 3 個になり、Google レビュー回数が増える前提）。Marketplace 公開（Phase F）のクリティカルパスはここ。

#### GitHub 連携の認証

- PdM が GitHub Personal Access Token（または Fine-grained PAT）を発行（`contents: write` スコープ、対象リポ限定推奨）
- Apps Script の `PropertiesService.getUserProperties().setProperty('GITHUB_PAT', '...')` に保存（**baseline §15: PdM 手作業**）
- 万一 PAT が漏洩しても影響範囲は `contents: write` の対象リポのみ。GitHub 側で revoke 可能

#### onEdit installable trigger

- Apps Script Editor の「トリガー」UI で `onSheetEdit` を installable trigger として登録（**PdM 手作業**、baseline §15）
- ScriptApp.newTrigger プログラム的登録もコード経由で可能だが、初回は OAuth 再同意モーダルが走るため PdM がブラウザで実行する必要あり
- 編集 → debounce 2 秒 → `validateSheet` → 通過行のみ `sheetToSchema` で md 再生成 → `UrlFetchApp.fetch(githubApi, ...)` で `PUT /repos/{owner}/{repo}/contents/{path}` 実行

#### conflict resolution

- **v0.7.0**: 最終編集勝ち（`If-Match` ヘッダなし、`sha` チェックなしで上書き）。ローカル md を編集して同時に Sheets も編集すると、Sheets 側が GitHub commit を上書きする可能性あり。PdM 1 人運用なら問題なし
- **v0.8.0 以降**: `sha` チェック + 3-way merge（複数ユーザ運用想定時）

### E-1 を v0.7.0 入りにすることのトレードオフ

| 項目 | v0.7.0 入り（PdM 採用） | v0.7.1 後送り（推奨案だった） |
|---|---|---|
| Phase E 検証申請所要日数 | 5〜7 週 | 3〜4 週（その後 v0.7.1 で再申請が必要） |
| Marketplace 公開タイミング | v0.7.0 完成 + 5〜7 週後 | v0.7.0 完成 + 3〜4 週後（onEdit は後続マイナーで） |
| PdM 体験 | 「シートを編集すると自動で md が GitHub commit + エラーが画面に出る」が初版から成立 | 手動 export ボタンが必須、初版は静的 |
| 実装複雑度 | 中（GitHub API client + installable trigger + debounce + validation 通知） | 低（手動同期のみ） |
| OAuth 検証申請の手間 | 1 回で済む | 2 回（v0.7.0 + v0.7.1）|

**PdM 判断採用理由**: Marketplace 公開は遅れるが、検証申請は 1 回で済み、PdM 体験は完成度が高い。長期的には合理的選択。

### スコープ境界（PdM 合意後、v0.7.0 確定版）

| 機能 | v0.7.0 | v0.7.1 以降 |
|---|---|---|
| frontmatter `columns` → DataValidation / ConditionalFormat / DateValidation 自動適用（import 時） | ✅ | |
| Sheets → md export 時に `columns` + `visual` を frontmatter として書き戻し | ✅ | |
| ヘッダー行固定 + 列幅自動調整 | ✅ | |
| イレギュラー検出（未知列 / enum 範囲外 / 型違反） + 通知（サイドバー + setNote） | ✅ | |
| **onEdit 自動同期（Sheets 編集 → GitHub API → md commit）** | ✅ | |
| GitHub PAT 管理（PropertiesService） | ✅ | |
| Chrome 拡張側のプレビュー（schema-test-spec plugin） | ✅ | |
| PDF 出力（A4 縦テーブル形式） | ✅ | |
| 3-way merge / conflict resolution | ❌ | **v0.8.0**（複数ユーザ運用時） |
| `required: true` 必須列バリデーション | ❌ | v0.7.x |

### 次セッションのタスク（実装着手）

PdM の最終 GO が出たら（or 「進めて」指示で）、以下の順で TDD 着手:

1. `packages/schema-test-spec/` の package.json + tsconfig + Ajv standalone code-gen 設定
2. `src/schema.ts`（frontmatter JSON Schema、Ajv 対応）+ `test/schema.test.ts`（失敗テスト先行）
3. `src/normalize.ts` + `src/autofill.ts` + `src/parseTestSpec.ts` の TDD
4. `apps/google-workspace-addon/src/lib/schemaToSheet.ts` + `sheetToSchema.ts` + `validateSheet.ts`（Apps Script 環境モックを駆使）
5. `apps/chrome-extension/src/plugins/test-spec.ts`
6. `packages/renderer-pdf/src/testSpecTemplate.ts`
7. onEdit / GitHub PAT は最後に（CI で UrlFetchApp モック）

### 2026-06-18 Phase 1 TDD GREEN（`packages/schema-test-spec/` 雛形完成）

実装した範囲（schema-spec の構成にミラー）:

- `package.json` — `@md-business/schema-test-spec` workspace package、exports `.` / `./schema` / `./validate` / `./schema.json`、deps = `@md-business/core` workspace + ajv 8 + ajv-formats 3 + vitest 2.1 + v8 coverage
- `tsconfig.json` / `tsconfig.build.json` — root tsconfig 継承、build 用は `src` 限定
- `vitest.config.ts` — v8 coverage、しきい値 80（全 4 軸）、`index.ts` / `types.ts` / `schema.ts` は除外（再エクスポート / 型のみ）
- `scripts/build-validate.mjs` — Ajv standalone code-gen + `require()` → `import * as` 巻き上げ（MV3 CSP 互換維持、schema-spec と同パターン）
- `src/test-spec.schema.json` — JSON Schema draft 2020-12、`schema: const "test-spec/v1"`、`columns: minItems: 1` 必須、7 種列型、`if type=enum then required: values` 条件付き必須、`visualStyle.row_background|background|color` のみ + hex pattern、`additionalProperties: false` 全層
- `src/types.ts` — `TestSpec` / `TestSpecColumn` / `ColumnType` / `ColumnVisualStyle` / `TestSpecPerson` / `TestSpecStatus = draft|review|executing|completed`
- `src/schema.ts` — `SCHEMA_VERSION = 'test-spec/v1' as const` + `testSpecSchema`
- `src/dictionary.ja.ts` — 4 scope (`root|party|column|visualStyle`) 辞書 + `STATUS_TRANSLATIONS` + `COLUMN_TYPE_TRANSLATIONS`（文字列/プルダウン/日付/数値/チェックボックス/URL）+ `THEME_VALUE_TRANSLATIONS`
- `src/normalize.ts` — `translateLeaf` に **`column.visual` の dict-of-visualStyle 特別扱い**（外側 enum キーは保持、内側のみ visualStyle scope で再帰）を追加。初版で `childScopeFor` だけだと外側キーまで辞書 lookup されてしまうバグを 1 サイクルで検出 → 修正
- `src/autofill.ts` — defaults (`schema/version/status`) + 列名重複警告 + enum without values 警告 + visual キーが values にない警告
- `src/parseTestSpec.ts` — `splitFrontmatter → normalize → autofill → validateWithCompiled` パイプ、`validate` は DI で MV3 CSP runtime-free

テスト結果（`pnpm --filter @md-business/schema-test-spec test:run`）:

- 4 テストファイル / **74 件全緑**
- カバレッジ Stmts **100%** / Branch **94.87%** / Funcs **100%** / Lines **100%**（しきい値 80 全 4 軸クリア）
- `pnpm --filter @md-business/schema-test-spec typecheck` も通過

次のフェーズ（Phase 2 着手予定）:

- `src/translateError.ts` — Ajv 出力エラーの日本語化（schema-spec / schema-invoice と同じパターン: path × keyword → 日本語ラベル）
- `src/fileName.ts` — `{documentNumber}` `{title}` 等 token 展開（Windows 禁止文字サニタイズ）
- `templates/test-spec/standard-ja.md` — Phase 3 で Apps Script schemaToSheet / sheetToSchema / validateSheet を実装する際の fixture

### 2026-06-18 Phase 2 TDD GREEN（`translateError` + `fileName` 完成）

実装した範囲（schema-spec を test-spec 文脈に適合）:

- `src/translateError.ts` — `TEST_SPEC_FIELD_LABELS` に test-spec 固有 path（`/schema` / `/columns/N` / `/columns/N/name` / `/columns/N/type` / `/columns/N/values` / `/columns/N/values/N` / `/columns/N/visual` / `/columns/N/visual/X/{row_background|background|color}` / `/columns/N/min` / `/columns/N/max` / `/googleSheetId` / `/theme` / `/fileName`）を登録。**`X` placeholder** を導入し `column.visual` 配下の enum 値（OK/NG/保留 等）をエラーラベルに動的反映（`normalizePath` で visual 直下の非数字キーを `X` に折り畳み、`applyIndices` で `{X}` を埋める）。`PATTERN_HINTS` に SemVer + hex 色 3 種、`FORMAT_HINTS` に `/issueDate`、`ALLOWED_VALUES` に status (`draft/review/executing/completed`) + `column.type` (`text/multiline_text/enum/date/number/checkbox/url`)。`additionalProperties` は親 path の label を直接表示（列ノード内の未知キーで「列[N+1] に未知のキー…」となる）。`translateTestSpecError` + `translateTestSpecErrors` + `translateTestSpecWarning` + `translateTestSpecWarnings` の 4 関数を export
- `src/fileName.ts` — token 展開（`{文書番号}/{documentNumber}` / `{タイトル}/{title}` / `{版}/{version}` / `{ステータス}/{status}` / `{発行日}/{issueDate}` / `{発行日YMD}/{issueYMD}` / `{YMD}` / `{date}/{今日}`）+ Windows 禁止文字（`/ \ : * ? " < > | \r \n \t`）→ `_` サニタイズ + 連続 `_` 折り畳み + 前後 trim。**デフォルトテンプレ = `検証シート_{文書番号}_v{版}`**（schema-spec の「基本設計書_…」と並ぶ命名）
- `src/index.ts` — 上記 5 関数を re-export

テスト（`test/translateError.test.ts` 25 件 + `test/fileName.test.ts` 14 件）:

- 全 113 件緑（Phase 1 の 74 件 + Phase 2 の 39 件）
- カバレッジ Stmts 99.63% / Branch 82.19% / Funcs 100% / Lines 99.63%（しきい値 80 全 4 軸クリア）
- `pnpm --filter @md-business/schema-test-spec typecheck` 通過

次のフェーズ（Phase 3 着手予定）:

- `templates/test-spec/standard-ja.md` — frontmatter sample（schema-spec の standard-ja.md と同じ位置付け、Phase 3 fixture 兼 templates 起動 UX のサンプル）
- `apps/google-workspace-addon/src/lib/schemaToSheet.ts` + `sheetToSchema.ts` + `validateSheet.ts` — D-1 確定設計の実装（イレギュラー検出 + 通知）
- `apps/chrome-extension/src/plugins/test-spec.ts` + `packages/renderer-pdf/src/testSpecTemplate.ts` — viewer + PDF 経路

### 2026-06-18 Phase 3A TDD GREEN（`renderer-pdf` testSpecTemplate + templates fixture 完成）

実装した範囲（`schema-spec` の specTemplate / renderSpecHtml と同パターンを test-spec 文脈に適合）:

- `packages/renderer-pdf/src/testSpecTemplate.ts` — `renderTestSpecBody(spec, options)`。表紙 = ステータスバッジ 4 種 (draft/review/executing/completed) + meta dl + 作成者 / レビュアー / 関連文書 / Google Sheets ID の各セクション、未設定時は省略。列定義セクション = column の name / type ローカライズ (text→文字列 / multiline_text→複数行 / enum→プルダウン / date→日付 / number→数値 / checkbox→チェックボックス / url→URL) + enum 値の `(A / B / C)` 表記 + number range の `[min 〜 max]` 表記 + visual 書式を `<ul>` で enum キーごとに「行背景色 / 背景色 / 文字色」ラベル付き表示。body section は caller の `bodyHtml` を注入。Theme は invoice/spec と同じ preset 7 色 + hex 受付、CSS injection を `HEX_COLOR` で拒否。`hideCover` オプションで表紙抑制。`data-schema-version="test-spec/v1"` を escape して出力
- `packages/renderer-pdf/src/renderTestSpecHtml.ts` — `renderTestSpecBody` を `<!doctype html>` + `<html lang>` + `<head>` (title / meta / styles) + `<body>` でラップする full HTML wrapper。`embedStyles` で inline CSS、`stylesHref` で `<link rel="stylesheet">` 添付、`documentTitle` で title 上書き、`lang` で language 上書き。`renderSpecHtml` と同パターン
- `packages/renderer-pdf/src/styles/test-spec.css` — `spec.css` を base に `.mdb-spec` → `.mdb-test-spec` 系に書き換え + 列定義表 (`mdb-test-spec__columns-table`) / Sheet ID セクション (`mdb-test-spec__sheet`) / visual list (`mdb-test-spec__visual-list`) を追加。表紙は spec と同じ 297mm 縦カラーバー、列定義は表紙の次ページ (`page-break-before: always`)、本文 (検証表) は次々ページ
- `packages/renderer-pdf/src/index.ts` — 上記 2 関数 + 2 型 を re-export
- `packages/renderer-pdf/package.json` — `@md-business/schema-test-spec: workspace:*` を deps に追加 + `exports./styles/test-spec.css` を追加
- `templates/test-spec/standard-ja.md` — ログイン機能 検証シート (7 列構成: 項目 / 手順 / 期待結果 / 結果 (プルダウン OK/NG/保留/未実施) / 実施日 (日付) / 担当 / 備考) + visual NG=行赤 / 保留=セル黄 / OK=文字緑 + 検証行 6 件 (正常ログイン / パスワード誤入力 / アカウントロック (NG) / パスワードリセット / Remember Me (保留) / ソーシャルログイン (未実施))。frontmatter 全キーを日本語表記、Phase 3B/3C の fixture も兼ねる

テスト（`packages/renderer-pdf/test/testSpecTemplate.test.ts` 27 件 + `test/renderTestSpecHtml.test.ts` 9 件、合計 36 件追加）:

- renderer-pdf 全 **148 件緑** （既存 112 件 + 新規 36 件）
- カバレッジ Stmts **98.38%** / Branch **89.87%** / Funcs **100%** / Lines **98.38%**（しきい値 80 全 4 軸クリア）
- `pnpm --filter @md-business/renderer-pdf typecheck` 通過

次のフェーズ（Phase 3B 着手予定）:

- `apps/google-workspace-addon/src/lib/schemaToSheet.ts` — frontmatter columns → DataValidation + ConditionalFormat + DateValidation + setFrozenRows(1)
- `apps/google-workspace-addon/src/lib/sheetToSchema.ts` — Sheets values + 既存 frontmatter → md round-trip
- `apps/google-workspace-addon/src/lib/validateSheet.ts` — D-1 確定設計 (unknown_column / enum_out_of_values / invalid_date / 型違反 を検出 → setNote + サイドバー通知)

その後 Phase 3C: onEdit installable trigger + GitHub API client + chrome-extension `plugins/test-spec.ts`。

### 2026-06-18 Phase 3B TDD GREEN（Apps Script schemaToSheet / sheetToSchema / validateSheet 完成）

実装した範囲:

- `apps/google-workspace-addon/src/lib/testSpecSheetOps.ts` — **副作用ゼロの純粋関数層**:
  - `planSheetWriteOps(spec)` → `SheetWriteOps` (Sheets 書き込み計画の中間表現):
    - `headerValues: string[]` — 列名
    - `frozenRows: 1`
    - `dataValidations: SheetDataValidationOp[]` — `kind = 'list' | 'date' | 'number' | 'checkbox' | 'url'` + 必要に応じて `values` / `min` / `max`
    - `conditionalFormats: SheetConditionalFormatOp[]` — `scope = 'row' | 'cell'` + `matchValue` + `color` / `backgroundColor`
  - `applySheetValuesToSpec(spec, values)` → `{ spec, body }` (spec は不変、`body` は valuesToMdTable で生成)
  - `validateSheetValues(spec, values)` → `SheetValidationIssue[]` (D-1 確定設計):
    - `unknown_column` — header にあるが spec.columns に無い列名
    - `missing_column` — spec.columns にあるが header に無い列名（`col: 0`）
    - `enum_out_of_values` — enum 列で values にない値
    - `invalid_date` — ISO-8601 (`YYYY-MM-DD`) でも Date オブジェクトでもない
    - `invalid_number` — 数値として解釈不可
    - `number_out_of_range` — min/max 範囲外
    - `invalid_checkbox` — TRUE/FALSE/true/false/真偽値以外
    - `invalid_url` — http(s):// 以外
    - 1-indexed の row/col + headerName + cellValue を返却
- `apps/google-workspace-addon/src/main.ts` に Apps Script trigger 層 を追加:
  - `setupTestSpecSheet(markdownFrontmatter: string)` — frontmatter parse → planSheetWriteOps → SpreadsheetApp.newDataValidation / newConditionalFormatRule で適用 + setFrozenRows(1)
  - `validateActiveTestSpecSheet(markdownFrontmatter: string)` — validateSheetValues 結果を `setNote` で該当セルに注釈 (日本語化) + サイドバーに issue 一覧返却
  - `exportTestSpecMarkdown(markdownFrontmatter: string)` — applySheetValuesToSpec → frontmatter + body 結合 md を返却
- `apps/google-workspace-addon/src/sidebar.html` に 3 ボタン追加（検証シート: セットアップ / 検証 / md 書き出し）+ placeholder を YAML frontmatter 例に更新
- `apps/google-workspace-addon/package.json` に `@md-business/schema-test-spec: workspace:*` を追加
- parseTestSpec は schema-test-spec の compiled validator (`@md-business/schema-test-spec/validate` の default export) を DI で渡す

テスト（`apps/google-workspace-addon/test/testSpecSheetOps.test.ts` 36 件）:

- google-workspace-addon 全 **49 件緑**（mdTable 13 + testSpecSheetOps 36）
- カバレッジ Stmts **98.73%** / Branch **90.51%** / Funcs **100%** / Lines **98.73%**（しきい値 80 全 4 軸クリア）
- `pnpm typecheck` 通過 / esbuild bundle **180.1kb** 成功
- turbo `typecheck` + `test:run` 全 21 task 緑

設計の要点:

1. **計画と適用の分離** — SpreadsheetApp API は test 環境で mock しづらく副作用も多い。SheetWriteOps という中間表現を純粋関数で生成し、Apps Script trigger 層はそれを薄く流すだけにすることで、ロジックは全て test 可能な層に閉じる
2. **D-1 確定設計を「シート側の値を壊さない」設計に** — 検出した issue は `setNote` で該当セルに注釈を付与し、サイドバーに件数 + 一覧を返すのみ。シート上の値そのものは編集しない（PdM が手動で修正するか、frontmatter columns を更新するかの判断を残す）
3. **row_background は formula 条件付き書式** — `=$<colLetter>2="<enumValue>"` を `whenFormulaSatisfied` で全列に適用し、background/color は単一セルの TEXT_EQ 条件付き書式に分離（visual scope と Apps Script API の API 形状が 1:1 にならないが、純粋関数層で吸収）
4. **schema-test-spec の compiled validator は default export** — `validate.compiled.d.ts` が `export default validate;` のため、`import validateTestSpec from '@md-business/schema-test-spec/validate'` で受け取る（最初に named import で typecheck エラーになり 1 サイクルで気づいて修正）

次のフェーズ（Phase 3C 着手予定）:

- `apps/google-workspace-addon/src/main.ts` に onEdit installable trigger + debounce (`LockService` + 時間ベース trigger) を追加
- GitHub API クライアント (`UrlFetchApp` + `PropertiesService.getUserProperties().getProperty('GITHUB_PAT')`) で md 自動 commit。PAT 投入は baseline §15 により PdM 手作業
- `apps/chrome-extension/src/plugins/test-spec.ts` を追加し、検証シート md をブラウザで開くと `renderTestSpecHtml` + Paged.js で A4 縦 PDF として描画

### 2026-06-18 Phase 3C-1 TDD GREEN

Phase 3C を 3 サブフェーズに細分化 (baseline §7「フェーズが大きすぎないか」+ §20 チェーン順序):

| サブフェーズ | スコープ |
|---|---|
| **3C-1**（本セッション完了） | GitHub API クライアント純粋関数 (`parseRepoRef` / `buildContentsUrl` / `buildContentsPutPayload`) のみ |
| 3C-2（次セッション） | onEdit installable trigger + UrlFetchApp + PropertiesService PAT 経由の md 自動 commit |
| 3C-3（その次） | `apps/chrome-extension/src/plugins/test-spec.ts` で renderTestSpecHtml + Paged.js プレビュー / DL |

実装（Phase 3C-1）:

- `apps/google-workspace-addon/src/lib/githubApi.ts` 新規:
  - `parseRepoRef(input: string): RepoRef | null` — `owner/repo@branch:path` を分解。`@branch` 省略時は `branch = 'main'`。branch スラッシュ (`feat/test-spec`) 対応。owner / repo / path が空、または `/` / `:` 未含の入力は `null`。前後空白 trim
  - `buildContentsUrl(ref): string` — `https://api.github.com/repos/{owner}/{repo}/contents/{encodedPath}?ref={encodedBranch}` を組む。path はセグメントごと `encodeURIComponent`（`/` 保持）、branch は `encodeURIComponent` で `feat/foo bar` → `feat%2Ffoo%20bar`
  - `buildContentsPutPayload({message, contentBase64, branch, sha?, committer?})` — Contents API PUT body を組む。`sha` 省略時は `sha` key 自体を payload に含めず（新規作成）、提供時のみ追加。`committer` optional。`contentBase64` はそのまま `content` に転記

テスト（`apps/google-workspace-addon/test/githubApi.test.ts` 14 件）:

- `parseRepoRef`: owner/repo@branch:path / branch 省略時デフォルト main / 入れ子 path / branch スラッシュ / path 欠落 null / owner-only null / 空文字 null / 前後 trim
- `buildContentsUrl`: 基本形 / path のスペースと日本語の percent encode（`/` 保持確認）/ branch スラッシュ encode
- `buildContentsPutPayload`: sha 無し時の payload キー / sha 有り時の sha 含む / `Object.prototype.hasOwnProperty` で sha key 自体の不在検証 / committer 保持

- google-workspace-addon 全 **63 件緑**（mdTable 13 + testSpecSheetOps 36 + githubApi 14）
- カバレッジ：githubApi.ts 単体 Stmts **100%** / Branch **86.36%** / Funcs **100%** / Lines **100%**、全体 Stmts **98.97%** / Branch **89.85%** / Funcs **100%** / Lines **98.97%**（しきい値 80 全 4 軸クリア）
- turbo `typecheck` + `test:run` 全 14 task 緑

設計の要点:

1. **Apps Script ランタイム依存ゼロ** — `Utilities.base64Encode` は純粋関数に持ち込まず、`contentBase64` を呼び出し側で受け取る引数化。Phase 3C-2 の trigger 層で `Utilities.newBlob(md).getBytes()` → base64 を呼び、本関数群に渡す
2. **GitHub binding の置き場所判断を保留** — frontmatter (`repository: "meta-taro/md-business@main:verify/login.md"` を schema に optional 追加) と PropertiesService の二択は Phase 3C-2 着手前に PdM 確認。本フェーズは純粋に文字列 ref をパースする / URL を組む / payload を組む の 3 関数に切り出すことで、どちらの置き場所に決まっても本関数群を再利用可能
3. **`parseRepoRef` の戦略** — 最初の `/` で owner を確定 → `@` 有無で branch/path 分岐 → branch スラッシュ含む場合は最初の `:` で path 分離。`feat/test-spec:verify.md` は path 側に `:` が出てきても誤誘導されないよう、`indexOf(':')` を branch-path 部分文字列に限定して呼ぶ

次のフェーズ（Phase 3C-2 着手前の PdM 確認事項）:

- **質問**: 検証シートの GitHub 同期先 (`owner/repo@branch:path`) をどこに保存するか
  - 案 A: frontmatter に `repository?: string` フィールド追加（schema 拡張・後方互換 OK・PdM が md でリポ別に管理可能）
  - 案 B: PropertiesService に `GITHUB_BINDING_<sheetId>` を保存（schema 不変・PdM がシート別に Apps Script UI で設定する必要あり）
  - **AI 推奨 = 案 A**（PdM が md ファイルでバインディング先を可視化・git 管理できるため）

## 結果

Phase 1 + Phase 2 + Phase 3A + Phase 3B + Phase 3C-1 完了。Phase 3C-2/3C-3 は次セッション以降（3C-2 着手前に GitHub binding 置き場所判断を PdM に確認）。
