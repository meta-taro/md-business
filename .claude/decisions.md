# Decisions — md-business

司令塔リポ側 `projects/md-business/decisions.md` が正本。本ファイルは配布リポでの作業時参照用ミラー。

| 日付 | 決定 | 理由 |
|---|---|---|
| 2026-06-18 | v0.7.0 Phase 3C-1 TDD GREEN: GitHub API クライアント純粋関数を `apps/google-workspace-addon/src/lib/githubApi.ts` に実装。`parseRepoRef(ref)` (`owner/repo@branch:path` 形式、`@branch` 省略時 `main` / branch スラッシュ対応 (`feat/test-spec`) / owner・repo・path 空で `null` / 前後空白 trim) + `buildContentsUrl(ref)` (GitHub Contents API URL、path はセグメントごと `encodeURIComponent` (`/` 保持)、branch も `encodeURIComponent` で query encode) + `buildContentsPutPayload({message, contentBase64, branch, sha?, committer?})` (sha 省略時は key 自体を除外して新規作成扱い / committer optional)。14 件のテスト追加で google-workspace-addon 全 63 件緑、しきい値 80 全 4 軸クリア (githubApi.ts 単体 Stmts 100% / Branch 86.36% / Funcs 100% / Lines 100%、全体 98.97% / 89.85% / 100% / 98.97%)、turbo 14 task 全緑 | Phase 3C を 3 サブフェーズに細分化 (3C-1=純粋関数 / 3C-2=onEdit + UrlFetchApp / 3C-3=chrome-extension plugin) して baseline §7 「小さいフェーズ」「フェーズが大きすぎないか」と §20 ブロックチェーン順序を守る。3C-2 着手前に判断が必要な「GitHub binding (repo/branch/path) をどこから取るか」(frontmatter `repository` 追加 vs PropertiesService) を保留したまま、純粋関数として文字列 ref のパース + URL/payload 構築を分離。Apps Script の `Utilities.base64Encode` を純粋関数に持ち込まず `contentBase64` を呼び出し側で受け取る引数化することで、Apps Script ランタイム依存をゼロに保ち、test 環境で `Utilities` mock を要求しない |
| 2026-06-18 | v0.7.0 Phase 3B TDD GREEN: `apps/google-workspace-addon/` に検証シート ⇔ Sheets 双方向同期と D-1 確定設計のバリデーションを追加。**純粋関数層** `src/lib/testSpecSheetOps.ts` = `planSheetWriteOps(spec)` (列名 / FrozenRows / DataValidation (enum→list / date / number+min+max / checkbox / url) / ConditionalFormat (row_background→row scope / background→cell scope / color→cell scope) の中間表現を返す) + `applySheetValuesToSpec(spec, values)` (values 配列を valuesToMdTable で md table body に変換、spec 自体は不変) + `validateSheetValues(spec, values)` (D-1 確定設計 = `unknown_column` / `missing_column` / `enum_out_of_values` / `invalid_date` (ISO-8601 + Date オブジェクト両対応) / `invalid_number` / `number_out_of_range` / `invalid_checkbox` (TRUE/FALSE/true/false/boolean 受付) / `invalid_url` (http/https) を行 / 列 1-indexed で返却)。**Apps Script trigger 層** `src/main.ts` に `setupTestSpecSheet` / `validateActiveTestSpecSheet` (issue ごとに `setNote` を該当セルに付与) / `exportTestSpecMarkdown` を追加。サイドバーに 3 ボタン追加。36 件のテスト追加で google-workspace-addon 全 49 件緑、しきい値 80 全 4 軸クリア (Stmts 98.73% / Branch 90.51% / Funcs 100% / Lines 98.73%)、esbuild bundle 180.1kb | Apps Script の SpreadsheetApp API は test 環境で mock しづらく副作用も多いため、**中間表現 (SheetWriteOps) を返す純粋関数** に切り出し、Apps Script trigger 側は薄く流すだけ。中間表現は `kind` / `scope` などラベル化することで Apps Script API 形式 (list / date / number) と 1:1 対応しつつ、テスト側は単純な JSON 比較で済む。D-1 (検出して通知) を「捨てない／取り込まない」設計として `setNote` + サイドバー通知に閉じ、シート側の値は壊さない。Phase 3C で onEdit + GitHub API auto-commit のフックを追加するときも、本関数群を呼び出すだけで済む拡張性を担保 |
| 2026-06-18 | v0.7.0 Phase 3A TDD GREEN: `packages/renderer-pdf/` に検証シート向け A4 縦テンプレ実装。`testSpecTemplate.ts` (renderTestSpecBody = 表紙 + 列定義テーブル + body 注入) + `renderTestSpecHtml.ts` (full HTML wrapper) + `styles/test-spec.css` (`spec.css` を base に `.mdb-test-spec` 系へ書き換え + 列定義表 / Sheet ID セクション追加) + `templates/test-spec/standard-ja.md` (7 列 + visual NG=行赤 / 保留=セル黄 / OK=文字緑 + 検証行 6 件)。表紙のステータスバッジは draft/review/executing/completed の 4 種、列定義表は type をローカライズ (text→文字列 等) + enum 値の `(A / B / C)` + visual を `<ul>` で「行背景色 / 背景色 / 文字色」ラベル付き表示。Theme は invoice/spec と同じ preset 7 色 + hex 受付、CSS injection を `HEX_COLOR` で拒否。36 件のテスト追加で renderer-pdf 全 148 件緑、しきい値 80 全 4 軸クリア (Stmts 98.38% / Branch 89.87% / Funcs 100% / Lines 98.38%) | `schema-spec` の specTemplate / renderSpecHtml と同じパターンを test-spec 文脈に適合。検証シートは「列定義 (frontmatter columns)」と「検証行 (md table)」の 2 階層が本質なので、表紙の直後に **列定義セクション** を `page-break-before: always` で独立ページ化し、本文の検証表 (caller の bodyHtml) と視覚的に分離。enum 値 / range / visual の人間可読化を PDF 上で完結させ、Sheets を開かなくても列スキーマがレビュー可能 |
| 2026-06-18 | v0.7.0 Phase 2 TDD GREEN: `packages/schema-test-spec/` に `translateError` + `fileName` を追加。`translateError` は `column.visual/<enum 値>/row_background` の `<enum 値>`（OK/NG/保留 等）をエラーラベルに動的反映するため **`X` placeholder** を `normalizePath`/`applyIndices` に追加（visual 直下の非数字キーを `X` に折り畳み、`{X}` を埋める）。`fileName` のデフォルトテンプレは `検証シート_{文書番号}_v{版}`。25 + 14 件のテスト追加で全 113 件緑、しきい値 80 全 4 軸クリア | schema-spec / schema-invoice と同じ翻訳 / 命名パターンを test-spec 文脈に適合。`visualStyle` が dict-of-visualStyle 構造を取る都合上、エラー path に enum 値が混入するため、`/columns/N/visual/X/...` の正規化が必要（label table を爆発させない設計） |
| 2026-06-18 | v0.8.0 で `schema-db-spec`（RDB、型プルダウン + ER 図）と `schema-nosql-db-spec`（NoSQL、ツリー / インデント主軸）を **同時並行実装** で確定（PdM 2026-06-18）。NoSQL は **JSON 連想配列に近い構造を素直に YAML 入れ子で宣言** → 枝記号付き階層表示 + 型バッジ + `*required` マーカー。access pattern 表は副次要素に格下げ。**第一参照源 = `coveooss/json-schema-for-humans`**（JSON Schema → 折りたたみツリー HTML、SIL/Apache 系 license の OSS）。engine 候補 = mongodb / firestore / dynamodb / cosmos / cassandra / neo4j / redis。PDF は **A4 縦**（先行案の A4 横は table-heavy 想定で立てたが、ツリー主軸では縦長階層と相性が良い）。v1 は折りたたみなしで全展開固定 | PdM 指示「NoSQL は JSON の連想配列に近いイメージなので、ツリーっぽかったり、インデントで表示できるみたいな設計書があると見やすい」。NoSQL は DB エンジンごとに構造が違いすぎて単一テンプレ化が難しいが、共通項は **JSON-like 階層構造の可視化** に絞れる。RDB と NoSQL を 1 フェーズに束ねるのは、DB 設計フェーズ全体として 1 つの Marketplace update に乗せる方が PdM 体験として連続性があるため |
| 2026-06-12 | リポは `meta-taro/md-business`（個人 GitHub・public・MIT） | OSS は個人アカウント運営方針 |
| 2026-06-12 | モノレポ採用（pnpm workspace + Turborepo） | スキーマ⇔ビューワーが 1:1 で密結合・同期リリース必須・fork 容易性 |
| 2026-06-12 | npm scope は `@md-business/` 推奨 | GitHub Org `mdbiz` が既存。Phase 1 公開時に `npm view` で実機確認 |
| 2026-06-12 | PDF レンダラを 2 系統並立: ブラウザ = Paged.js / サーバ = Typst | Typst の WASM ブラウザ動作は重い・Paged.js は HTML/CSS でハッカブル・Typst は inboisu で日本のインボイス対応即成立 |
| 2026-06-12 | 請求書スキーマは `mkpoli/typst-inboisu` を参照源とする | 唯一の現役 OSS で適格請求書要件を満たしている |
| 2026-06-12 | Chrome 拡張は `simov/markdown-viewer` を fork ベース | MV3 対応・GitHub raw MD レンダリング実装あり・MIT 互換 |
| 2026-06-12 | VS Code 拡張は `estruyf/vscode-front-matter` の Content Type 拡張 | 既存資産流用・frontmatter フォーム UI が完成度高い |
| 2026-06-12 | やる配布チャネル: Google Docs / Chrome / VS Code / PWA / LINE LIFF / GitHub Action | PdM 指示。非エンジニアから開発者まで網羅 |
| 2026-06-12 | やらない配布チャネル: Obsidian/Logseq / Word アドイン | PdM 指示 |
| 2026-06-12 | ライセンス MIT | OSS 王道・fork 自由を担保 |
| 2026-06-12 | UI は Lit Web Components + Tailwind CSS v4 | フレームワーク非依存・各 app に組み込みやすい |
| 2026-06-12 | コアは TypeScript | 全環境で使い回し可・型安全 |
| 2026-06-12 | 配布リポ担当者への外部委譲なし。司令塔セッションで実装 | 規模・思想・OSS 運営判断 |
| 2026-06-14 | Phase 1 を「請求書 md + Chrome 拡張のみ」に縮約（Phase 1-MVP）。`viewer-invoice` フォーム UI と `apps/pwa` は Phase 1b 以降へ移送 | PdM 指示。2026-06 中にキングダム社向け請求書フローを稼働させる目標 |
| 2026-06-14 | Chrome 拡張表示名は `md-business: Invoice (適格請求書)` | 日英併記・将来の `md-business: Test Spec` `md-business: Design Doc` 等への命名展開余地 |
| 2026-06-14 | 拡張への md 入力は拡張内インポート UI（ファイル選択） | 置き場所（git / Dropbox / ローカル）はユーザー判断と PdM 指示 |
| 2026-06-14 | 請求書 PDF は印影なしで出力。CSS で署名欄の余白のみ確保 | 適格請求書要件に印影は不要（法的義務なし）。商習慣のためのスペースのみ確保 |
| 2026-06-14 | 各社実データ（キングダム社等）は `.gitignore` で本家から排除（`/private/` `*.private.md` 等） | OSS 本家には汎用テンプレのみ。実値は別管理 |
| 2026-06-14 | Chrome Web Store 申請は public・ドコカデ社デベロッパーアカウントで実施 | 既存登録あり・OSS 本旨で公開可 |
| 2026-06-14 | Chrome 拡張は単発製品ではなく md-business シリーズ全体の受け皿。スキーマプラグイン構造で継続アップデート | PdM 指示。`v1.0.0=invoice`, `v1.1.0=+test-spec/design-doc`, `v1.2.0=+その他` |
| 2026-06-14 | Chrome 拡張の動作イメージ: ローカル md をブラウザで開いた瞬間に拡張が自動レンダリング → PDF DL | PdM 指示。`simov/markdown-viewer` の挙動を踏襲（明示的なインポートボタンに依存しない） |
| 2026-06-14 | test-spec スキーマの想定フィールド: 名前 / 日次チェック OK/NG / 備考欄（表形式） | PdM 指示。Phase 3 で実装 |
| 2026-06-14 | 将来構想（決定ではない）: ローカル単独編集 → 複数人同時編集への拡張余地を残す | PdM 指示。Phase 1b 以降で技術選定（候補: Yjs / Automerge / CRDT ベースの共同編集レイヤを PWA に組み込み） |
| 2026-06-15 | frontmatter パーサを `gray-matter` から `js-yaml` 直接呼び出しへ置換（`@md-business/core/splitFrontmatter`） | gray-matter は `lib/engines.js` に `eval(str)` を含み、未使用でもバンドルされる。Chrome MV3 の CSP `script-src 'self'` が `unsafe-eval` を拒否するため拡張が起動できなかった |
| 2026-06-15 | スキーマ検証は **Ajv standalone code generation** で各 schema パッケージが事前コンパイル（`@md-business/schema-invoice/validate`） | Ajv のランタイム `compile()` は `new Function()` を使い MV3 CSP 違反。standalone なら build 時に検証関数を ESM として吐き、ブラウザは関数を呼ぶだけ |
| 2026-06-15 | `@md-business/core` を browser-safe（`main`）/ node-only（`./runtime`）に分割 | runtime Ajv を引きずらないと chrome-extension のバンドルから `new Function()` を tree-shake で落とせない。`validateWithCompiled` のみ main から export。`validateWith`/`parseAndValidate` は `@md-business/core/runtime` に隔離 |
| 2026-06-15 | baseline 項目5 の Git hooks（pre-commit / pre-push）を Phase 0 で実装漏れしていたため Phase 1-MVP 中に補完。Husky 9 + `scripts/scan-bundle.mjs` 導入。pre-push 時にバンドルを走査し `eval` / `new Function` / `Function("...")` を検出 | Phase 0 で baseline 項目5 が書かれていたが実装されておらず、Chrome 拡張ビルドが MV3 CSP 違反を含むまま push されかけた。事前検知の不在が直接の原因。再発防止として静的検査ゲートを追加 |
| 2026-06-15 | Vite ビルドの rollupOptions.onwarn で `EVAL` warning を error 化 | 依存パッケージが将来 eval を持ち込んだ瞬間にビルドが落ちる。bundle scan より早く失敗するため二重防御 |
| 2026-06-15 | Ajv standalone 出力の生 `require()` 呼び出しを `scripts/build-validate.mjs` 内で top-level `import * as` に書き換える後処理を追加 | Ajv standalone は `esm: true` でも `ucs2length` / `ajv-formats` の helper を `require()` のまま吐く既知制約があり、ブラウザで `Uncaught ReferenceError: require is not defined` を起こした。post-process で全 `require("...")` を一意な binding に置換し、ファイル先頭に namespace import を追加することで、Vite/Rollup が正規の ESM として解決・tree-shake できる |
| 2026-06-15 | `scripts/scan-bundle.mjs` に `require("...")` 検出と「文字列/コメント内は除外」のヒューリスティック判定を追加 | 既存 scan は `eval` / `new Function` のみで、ブラウザバンドルに残る生 `require()` を見逃した。あわせて Ajv 内部の `Ue.code = 'require(...)'` メタ文字列や pagedjs のコメント `// require('util')` を誤検知しないよう、直前文字（quote）と行頭（`//` / `*`）を確認する除外を入れた |
| 2026-06-16 | PDF DL ボタンは Chrome 印刷ダイアログを自動起動するフロー（A 方針）。初回は「送信先を PDF として保存に切替」案内モーダルを出し、`mdb:pdf-guide-skip` を localStorage に保存して 2 回目以降スキップ可能 | 2026-06-14「印影なし方針」のあと、PdM から「PDF にできない人/PC もあるので DL ボタンは必須」の指示。MV3 拡張からの直接 PDF 生成は jspdf/pdf-lib 級の依存追加 + 日本語フォント埋込みになり Phase 1-MVP には重い。`window.print()` + 案内モーダルで「ユーザー手元 OS の PDF 出力」に流す方が軽量で確実 |
| 2026-06-16 | Noto Sans JP / Noto Serif JP を `@fontsource/*` 経由で拡張に同梱（`japanese` `latin` サブセット、必要 weight のみ）。`scripts/post-build.mjs` で woff2 を `dist/vendor/fonts/` にコピーし、`@font-face` を含む `dist/styles/fonts.css` を自動生成。viewer 起動時に `chrome.runtime.getURL('styles/fonts.css')` で動的読み込み | MV3 CSP は外部フォント CDN（Google Fonts 等）からの読み込みを拒否し、また審査面でも外部送信は不利。同梱式なら CSP `'self'` で成立。`@fontsource/*` は SIL OFL 1.1 で再配布可。`MdBusiness Body` / `MdBusiness Stamp` alias を追加し、各社 fork が `@font-face` を上書きするだけで篆書/楷書フォントに差し替えられる構造にした |
| 2026-06-16 | 篆書系 OSS フォントの同梱は現時点で不可と判定。Noto Serif JP Bold で代替し、`MdBusiness Stamp` alias を fork-friendly swap-point として保持 | 青柳衡山フォント T は「改変・再配布不可」のカスタムライセンスで MIT 配布リポに同梱できず、JFZSKSealScript は SIL OFL だが中国語専用で日本の仮名・JIS 1/2 漢字をカバーしない。日本語覆い率を満たす OFL 篆書フォントは現行エコシステムに存在しない。Phase 1.1 で再評価予定 |
| 2026-06-16 | ハンコ SVG ジェネレータを `packages/renderer-pdf/src/stamp.ts` に実装。`stamp.shape: auto` は法人キーワード（株式会社/有限会社/合同会社/合名会社/合資会社 + `(株)`系略号 + `㈱`系合字）を検出して角印、それ以外は丸印に自動判定。文字数で 1/2/3/4 字レイアウト分岐、ASCII テキストは横書き 1 行、CJK は縦書き/田の字。色は朱色 `#c8161d`、フォントは `MdBusiness Stamp` alias → Noto Serif JP fallback | PdM 指示「田中甫朋とか、株式会社Dokokade、株式会社キングダムを自動でハンコっぽく」。ハンコ画像を社員が用意せずとも適格請求書に体裁として印影を載せたいニーズ。SVG 1 個で完結するので追加バイナリ・CSP 影響なし。schema 側に `stamp?` フィールドを追加し、未指定ならデフォルトで非表示（2026-06-14「印影なし方針」と互換） |
| 2026-06-16 | schema-invoice v0.2: frontmatter を **日本語キー一級市民**化（英語キーも引き続き受け付け）。`taxSummary` / `totals` は `items[]` から自動算出。`taxRounding`（floor/round/ceil、既定 floor）で丸めを制御。`fileName` テンプレで PDF 保存名をカスタム可能 | 手書き運用での視認性・誤計算リスク低減。適格請求書は「税率ごとに 1 回丸める」compliance ルールがあり手計算は事故ベース。日本語キーは AI 生成時の精度も上がる。内部 TS 型は英語維持（OSS の海外フォーク・lint メッセージ可読性のため）。正規化・自動計算は schema-invoice 内の純粋関数 `normalizeInvoiceFrontmatter` / `autofillInvoice` で実装し validate の前段に挿入 |
| 2026-06-16 | 請求書 frontmatter の正本用語表（揺れ防止）を凍結。**ルートキー**: 請求書番号 / 発行日 / 支払期限 / 発行元 / 請求先 / 品目 / 振込先 / 備考 / 印影 / 丸め / ファイル名。**当事者キー**: 名前 / 敬称 / 登録番号 / 郵便番号 / 住所 / 電話 / メール。**品目キー**: 名前 / 数量 / 単位 / 単価 / 税率 / 軽減税率 / 備考。**振込先キー**: 銀行 / 支店 / 種別 / 口座番号 / 名義。**印影キー**: 有効 / 形 / 文字 / フォント。これ以外の synonym（名称・商号・氏名 等）は `dictionary.ja.ts` で正本へマッピング、衝突時は警告 | 同義語が複数許容されると Markdown 全体で揺れが生まれ、AI 生成と人間編集の二重管理になる。正本を 1 セットに絞り、別名は normalize レイヤで吸収する設計。ドキュメント・ChatGPT プロンプトで参照する基準語彙としても使う |
| 2026-06-16 | frontmatter に `theme`（青/赤/黄/橙/紫/黒/灰 プリセット or `#RRGGBB`）と `logo`（`data:image/{png,jpeg,gif,webp};base64,...` or `https://...`）を追加 | OpenAI / Canva 等の請求書 PDF はロゴ + テーマ色がデフォ。各社が CSS を触らず frontmatter 一行でブランディングできるようにする。`theme` は preset/hex 厳格マッチ、`logo` は data URI と HTTPS の regex ホワイトリストのみ受け付け（XSS / CSS injection 防御、SVG は意図的に拒否） |
| 2026-06-16 | Chrome 拡張 manifest 権限を `storage` のみに最小化（`activeTab` / `scripting` を削除） | 実コードで `chrome.tabs.*` や `chrome.scripting.executeScript` を呼んでおらず、`content_scripts` は静的注入のため `scripting` 権限不要。最小権限原則 + Chrome Web Store 審査の理由欄削減 + ユーザーインストール時の警告削減 |
| 2026-06-16 | Privacy Policy URL は GitHub の `PRIVACY.md` blob URL を直リンク（`https://github.com/meta-taro/md-business/blob/main/PRIVACY.md`） | データ収集ゼロ・完全ローカル動作なので長文ポリシーは不要、リポと同期されるシンプルな運用。GitHub Pages を立てる工数を省略。OSS 監査性も高い |
| 2026-06-16 | Chrome Web Store 申請パッケージ生成は自前 Pure Node zlib スクリプト（`apps/chrome-extension/scripts/make-release.mjs`）。`source-map` / OS junk / 既存 `.zip` `.crx` を除外、`dist/` 配下を archive root に展開 | `zip` / `7z` バイナリの可用性に依存しない再現性、外部依存追加なし（npm パッケージ抜きで CRC32 + DEFLATE 実装）、Chrome Web Store の「manifest.json が root」要件に合致する archive 構造 |
| 2026-06-16 | Chrome 拡張のバージョン展望を SemVer 起点に修正（旧: v1.0=invoice / v1.1=+test-spec、新: v0.1.0=invoice MVP / v0.2.0=+schema-spec / ... / v1.0=schema API 安定後の安定版宣言）。zip 差し替え毎に patch bump（0.1.0 → 0.1.1 → ...）、スキーマ追加は MINOR bump | Phase 1-MVP はβ段階、破壊変更余地を残したい。0.x の MINOR bump は SemVer 公認の破壊変更レーン。v1.0 マーケアピールは schema API 安定後に取っておく |
| 2026-06-16 | 印影 `extractStampChars` で姓名間の空白（半角 / 全角 / タブ、`\s+`）を除去するよう修正 | 「山田 太郎」が `['山','田',' ','太']` になり 4 文字目「郎」が落ちる、かつ空白がレイアウトに紛れる事象が `private/sample-contractor-invoice.md` で発覚。`renderStampSvg` は文字数で 1/2/3/4 字レイアウト分岐するので、空白を文字として扱うとレイアウトが破綻する |
| 2026-06-16 | 画像 / SVG / 概念図の取り扱い方針: 外部参照（`![](./figs/x.svg)` ローカル / `![](https://.../x.svg)` クラウド）+ インライン `<svg>` を基本。draw.io はネイティブレンダリングせず SVG エクスポート経由で扱う。Mermaid は ` ```mermaid ` コードブロックを動的 import で描画 | Phase 2 = 基本設計書スキーマ向け。draw.io ネイティブレンダラは XML パーサ + 図形ライブラリで実装コスト大、SVG/PNG エクスポート運用で 80% カバー可能。Mermaid は GitLab / GitHub 互換書式で AI 親和性が高く、テキスト diff が可能。mermaid.js を初期バンドルに混ぜると 200KB+ になるので、コードブロック検出時のみ動的 import |
| 2026-06-16 | popup に「テンプレートから始める」ボタンを追加（v0.1.1 patch スコープ）。`templates/invoice/*.md` を `dist/templates/` に同梱、クリックで viewer タブを untitled として開く | 第三者ユーザーが frontmatter をゼロから書くのは現実的でない（MVP のオンボーディング致命傷）。テンプレ始動 + viewer 内編集で「読み込んで編集する」フローを 1 クリックで提供する |
| 2026-06-16 | v0.1.1 の自動 version bump スクリプト `scripts/bump-version.mjs` を実装、純粋ロジックは `scripts/lib/version.ts` に分離し vitest でテスト可能に。`.mjs` CLI とは小さなロジック重複を許容 | vitest の esbuild 変換が `.ts` から `.mjs` 越境の `@ts-expect-error` をうまく扱えず、`.ts` 直接 import に切替が必要だった。CLI の方は Node 標準 ESM だけで動かしたい（build なしで実行可能）ため重複を許容、コメントで明示同期 |
| 2026-06-16 | Chrome 拡張開発を `feat/v0.2.0` ブランチで進める（main 温存）。`packages/schema-invoice` / `apps/chrome-extension/src/plugins/invoice.ts` / `packages/renderer-pdf/src/template.ts` の invoice 専用ロジック / `manifest.json` の version を触らない | v0.1.0 が Web Store 審査中で並行リリースになる可能性あり。invoice 系を不用意に書き換えると Web Store v0.1.x レーンと merge conflict + 退行リスク。差分ゼロを `git diff main -- ...` で確認する運用 |
| 2026-06-16 | v0.2.0 schema-spec の章ツリー設計を **B1 単一 md（自動目次）+ B3 chapters: 明示参照** 併用で確定。A 案（ファイル名 prefix で順序制御）は不採用 | A 案は `01-` `02-` の番号管理 + ASCII ツリー記号の罫線スペース調整が人間負担で運用破綻する。C 哲学（インデックス + 明示参照）なら `chapters: [./overview.md, ./architecture.md]` を 1 行足すだけで順序制御できファイル名に意味のある単語が入る。API 仕様書（v0.4.0+）も同じ C 哲学で行く |
| 2026-06-16 | v0.2.0 の Mermaid / SVG / 画像対応の技術選定: `mermaid@10.9.0`（動的 import で +200KB on-demand） / `dompurify@3.0.9`（+18KB、SVG / インライン HTML サニタイズ） / `remark-gfm@4.0.0`（+5KB、表・チェックボックス） / 自前 `sanitizeImageUrl()`（`data:` + `https:` のみ許可）。manifest CSP に `img-src 'self' data: https:` 追加、PDF は Paged.js + Mermaid 描画後の SVG をインライン埋め込みでテキスト検索可とする | 並行 Explore 調査（mdn/CSP 仕様 + mermaid issue tracker + dompurify changelog）から、MV3 + 拡張バンドルサイズ制約下で最も実績ある構成。bundle 影響合計 +218KB gzip のうち 200KB は on-demand のため初期ロード許容範囲。draw.io ネイティブレンダリングは見送り（SVG エクスポート運用で 80% カバー） |
| 2026-06-16 | v0.2.0 schema-spec の frontmatter テンプレ事例調査結果: MADR ライク（authors / reviewers / status / relatedDocs）+ Google Design Docs の 8 章構成ベース + arc42 とは違い prefix ではなく `chapters:` 明示参照、を採用方針として Issue 001 に記録 | MADR は ADR 単位の frontmatter 規約が SIL OFL の design-doc コミュニティで最普及、Google Design Docs は章数バランスが基本設計書に最適、arc42 の prefix 順序制御は上記 A 案不採用の理由と同じ |
| 2026-06-17 | Issue #12 結論: `pnpm-workspace.yaml` の `onlyBuiltDependencies: []` / `minimumReleaseAge: 1440` は正しく機能している。`node_modules/.modules.yaml` の `pendingBuilds: []`、`pnpm rebuild` が husky の prepare のみ実行、`pnpm install --frozen-lockfile` のログに `Thank you for using core-js` 等が一切現れないことで実証 | pnpm 公式（https://pnpm.io/settings）に従い `pnpm-workspace.yaml` が正しい設置場所と確認。`pnpm config get` は npm 互換の global config を見るコマンドで workspace settings を読まない仕様のため、設定の有効性検証には使えない。pnpm 11 で `onlyBuiltDependencies` は `allowBuilds` に renaming 予定 |
| 2026-06-17 | CI workflow に「baseline 1 検証ステップ」を追加（`.github/workflows/ci.yml`）。`pnpm install --frozen-lockfile` の出力を tee で保存し、`Thank you for using core-js` / `Running .* postinstall` / `<pkg>@<ver> postinstall` パターンを grep して検出時 fail | 設定が将来何らかの理由で外れた / pnpm のバージョンアップで挙動変化が起きた場合、CI で即時検出する。bundle scan（`scripts/scan-bundle.mjs`）と同方針の「既知の危険シグナルを stdout で監視する」方式 |
| 2026-06-17 | schema-invoice/v1 を **後方互換のまま拡張**: `issuer.registrationNumber` を optional 化（`issuer.required: ["name"]` のみ）、`issuer.taxExemptIssuer: boolean` を追加。autofill が both-set（登録番号 + 免税フラグ両方）/ neither-set（どちらもなし）を警告として通知。renderer-pdf が `taxExemptIssuer === true` を検知し「※ 適格請求書ではありません」（タイトル直下、赤字）+ インボイス制度経過措置（2023-10〜2029-9）案内（本文末尾、グレーボックス）を自動出力。`data-tax-exempt="true"` 属性も付与してテーマ別 CSS 拡張可能 | 免税事業者（屋号なし個人事業主・小規模法人等。例: 高橋たくと氏 → 株式会社キングダム宛 6 月分請求書）からの請求書を **schema-receipt 別パッケージを増やさず invoice/v1 のまま** 発行できるようにするため。B 案（新 schema パッケージ）はテンプレ・renderer・chrome-extension plugin の重複が大きく Phase 1.x スコープで過剰。A 案（v1 拡張）は新規ユースケースが optional フィールド追加のみで吸収可能。Issue #15 |
| 2026-06-17 | 日本語キー dictionary（`packages/schema-invoice/src/dictionary.ja.ts`）の party scope に `免税事業者` / `taxExemptIssuer` / `免税` / `非適格` / `インボイス未登録` → `taxExemptIssuer` を正本マッピングとして追加 | 「免税事業者: true」が AI 生成・人間編集ともに最も自然な日本語表現。複数の synonym を正本に集約することで揺れを抑え、`非適格` / `インボイス未登録` 等の現場用語もそのまま読める。frontmatter 用語表（2026-06-16 凍結）の延長線で同方針 |
| 2026-06-17 | `templates/invoice/tax-exempt-ja.md`（屋号なし個人事業主想定、90,000 × 10% → 99,000 円、`免税事業者: true`、taxSummary/totals は YAML コメントで参考値を残して autofill に委譲）を追加。chrome-extension popup の「テンプレートから始める」第 4 ボタンとして登録 | 免税事業者モードのオンボーディング致命傷を防ぐ。Web Store 審査者・新規ユーザーがゼロから書く必要なし。手書き参考値（90,000 / 9,000 / 99,000）を `feedback-invoice-manual-calc` 方針どおり YAML コメントで併記し、autofill 結果との対照確認も可能 |
| 2026-06-17 | v0.4.0 を 1 申請に統合（v0.2.0 sanitizer + v0.3.0 免税事業者モード + v0.4.0 popup 分岐 UX / 書き方ガイドモーダル）。Mermaid 対応は **v0.5.0+ へ延期**。`mermaid@10.x` 動的 import 案は **撤回** | Chrome MV3 CSP `script-src 'self'`（`unsafe-eval` 拒否）に対し、mermaid 11.x の依存（cytoscape / wardley / graph chunks）が `Function("return this")()` および `require("util").types` を含み、pre-push `scan-bundle.mjs` で検出（apps/chrome-extension/dist/chunks/{cytoscape, wardley, graph}.js の 7 箇所）。動的 import 化 + class-marker short-circuit でも該当 chunk 自体は bundle 内に残るため CSP 違反は解消できない。v0.5.0+ で代替手段（静的 SVG 埋め込み / 軽量代替ライブラリ / mermaid 完全 vendor 化）を再検討する。Issue #15 続報 |
| 2026-06-17 | v0.4.0 zip 生成は Mermaid 抜きの構成で実施（sanitizer + 免税事業者 + popup 分岐 + 書き方ガイド）。manifest.json / package.json は 0.1.1 → 0.4.0 へ単発 bump（v0.2.0 / v0.3.0 の中間 bump はスキップ、Web Store にはこれまで v0.1.0 しか公開していないため SemVer 連続性は問題なし） | 直前の Mermaid 統合コミット（renderMermaid.ts / viewer 統合 / spec.css）は pre-push で blocked → `git reset --hard b836ee5` で除去済み。残スコープ（sanitizer / 免税事業者モード / popup 分岐 / 書き方ガイド）は b836ee5 時点で完成しており、bump のみで Web Store 提出可能 |
| 2026-06-17 | 免税事業者請求書テンプレで `※ 適格請求書ではありません` のタイトル直下表記と `〜へ` 受領者サブタイトルを撤回（renderer-pdf）。本文末尾の経過措置案内（グレーボックス）は維持 | 商習慣上、受領者・発行者の双方にとって「あえて非適格と銘打って渡す」必要は薄く、むしろ取引相手に対して気まずさを生む。経過措置案内（本文末尾）+ 登録番号欄の空白で「適格でない」ことは十分に読み取れる。`taxExemptIssuer: true` の意味論は `data-tax-exempt="true"` 属性 + 経過措置案内の存在で残し、視覚的な朱書きを除去するのみ。あわせてレイアウトが 2 ページ目に溢れる問題も解消（PdM 検収フィードバック） |
| 2026-06-17 | `@md-business/core` の `renderMarkdownToHtml` に `remark-gfm@^4.0.0` を追加。表（pipe table）/ task list / 取り消し線 / autolink を GitHub Flavored Markdown 仕様で描画 | 基本設計書テンプレ `templates/spec/standard-ja.md` の「3.1 機能一覧」は pipe table で書かれており、GFM なしでは段落として生テキスト表示される（PdM 検収で発覚）。`remark-gfm` は純 JS（eval / new Function なし）で MV3 CSP セーフ、bundle 影響は viewer 側で約 +5KB gzip と小さい。手書き設計書の必需機能で、コスト < 効果 |
| 2026-06-17 | Chrome 拡張 viewer の PDF 印刷フローを「プレビュー iframe を上書きしてプリント」→「専用の隠し iframe を毎回作って印刷後に削除」に変更（`apps/chrome-extension/src/viewer/index.ts` `runPrintFlow`）。プレビュー iframe は触らない | 旧実装は印刷ダイアログを開く前に preview iframe を `doc.open(); doc.write()` で印刷 HTML に上書きし、`afterprint` で `bootstrapIframe` + `renderPreviewFromSource` の 2 段で復元していた。印刷ダイアログを **キャンセル** した際 Chrome のプレビュー teardown と復元処理が競合し、preview / editor の表示内容が消える事象が再現（PdM 検収で発覚: 「印刷をキャンセルするとブラウザ側のデータがなくなる」）。専用フレームならキャンセル時もメイン UI は一切変化せず、afterprint で隠しフレームを `remove()` するだけで完結 |
| 2026-06-17 | 基本設計書テンプレの表紙 1 ページ収まり + シャドウ削除（`packages/renderer-pdf/src/styles/spec.css`）。`@page :first` margin: 0、`.mdb-spec__cover` を `min-height/max-height: 297mm` + `overflow: hidden` で固定、title / meta / cover-inner / status の余白を 30〜40% 圧縮、`@media screen` の `box-shadow` / 外側マージンを撤去 | PdM 検収フィードバック「表紙ですが、余白はいらないですね」「表紙が 1 ページに収まってない」「mdb-spec にシャドウいらない」。表紙はカラーバー + 内側 padding で十分なデザイン余白があり、`@page` のページマージン + screen のシャドウは二重装飾になっていた。content 量（タイトル + 5 メタ項目 + 作成者 + レビュアー + 関連文書）でも A4 縦に収まることをサンプル `templates/spec/standard-ja.md` で確認 |
| 2026-06-17 | 免税事業者請求書の **2 ページ目はみ出し再発を CSS 圧縮で修正 + Playwright E2E 化**。`[data-tax-exempt='true']` スコープで `.mdb-invoice__transition-notice` margin/padding と `.mdb-invoice__signature` margin-top + `.seal-area` サイズを縮め、合計 ~11mm 圧縮。standard / inbound-eligible テンプレには影響しない。あわせて `packages/renderer-pdf` に `@playwright/test` + `pdf-parse` を導入し、`tests-e2e/invoice-fits-a4.spec.ts` で 3 テンプレすべてが A4 portrait 1 ページに収まることを `page.pdf({ preferCSSPageSize: true })` + pdf-parse numpages で検証。JSON-Schema 検証は ajv-compiled internal の Node ESM 解決問題で skip（schema-invoice の vitest が責任）、normalize + autofill のみ通す | スクリーンショット検収（2026-06-17 PM）で再発を確認。原因は v0.4.0 で追加した `transition-notice` グレーボックス（≈12mm）と既存 `fallbackSignature`（≈30mm）の累積が A4 portrait の text area（257mm）を僅かに超えていたこと。b3d9294 で「収まりやすくなる見込み」と書いたがゲートがなく、ブラウザ / フォントメトリクスの僅差で簡単に逆戻りする状態だった。Playwright E2E を入れたことで以降は物理ページ数で regression をブロックでき、過去 3 回繰り返した「1 ページ収まり崩壊」を恒久ゲート化。Issue #N（PdM スクショ報告） |
| 2026-06-17 | **renderer-pdf の `signatureArea` デフォルトを `true` → `false` に変更**（2026-06-14「印影なし方針」との整合性回復）。`packages/renderer-pdf/src/template.ts` の `RenderInvoiceBodyOptions.signatureArea` 既定値を反転、`fallbackSignature` の `<div class="seal-area">印</div>` 空欄が既定で出なくなる。`signatureArea: true` を明示渡したときのみ後方互換で復活。test/template.test.ts の「renders signature area by default」を「omits signature area by default」に反転、`signatureArea: true` で復活する側を追加。README の説明も反転 | 2026-06-14 decision で「請求書 PDF は印影なしで出力。CSS で署名欄の余白のみ確保」と方針確定していたが、template.ts のデフォルトが `true` のままで「印」と書かれた空欄が出ていた（PdM 検収 2026-06-17: `private/2026-06-contractor-invoice.md` の PDF で印が 2 ページ目に出る）。`private/2026-06-contractor-invoice.md` には `印影:` 行がそもそも書かれていなかったので、frontmatter 経由では消せない構造的不整合だった。renderer 側のデフォルト反転で 06-14 方針を徹底し、特定フォークが seal box を残したい場合は明示 opt-in できるよう後方互換を残す |
| 2026-06-17 | **Mermaid 図の `max-height` を 220mm → 190mm に再調整**（spec PDF p10 余白問題の解消）。db5f95f の 220mm 設定では p10 が「h1 + h2 だけで下半分余白、図は p11」状態だったため、PdM 検収で「図が次ページに行くのは大きいから？」確認 → 190mm に絞ることで「6. 画面遷移 + 6.1 注文確定フロー + フローチャート全体」を p10 に同居させた。15p → 14p → **13p** に圧縮。フローチャートのノード文字は 14px 程度残るため目視で読める範囲（PdM 検収「プレビューと極力一致」基準クリア）。ER 図 / コンポーネント図 / シーケンス図には引き続き regression なし | 220mm は「図単体がページ内に収まる」基準で、「見出しと図のセットがページ内に収まる」基準ではなかった。h1 (≈ 45mm) + h2 (≈ 15mm) + 図 (≤ 220mm) = 最大 280mm で本文有効高 257mm を超える → 図が必ず次ページに送られる。本文有効高 257 − h1 − h2 = 約 197mm が真の上限で、安全マージンを 7mm 取って 190mm に確定 |
| 2026-06-18 | **clasp v3 の `.claspignore` セマンティクス変更に対応**: パターン評価基点が v2 系の「プロジェクトルート相対」から v3 系では **`contentDir` (= `rootDir` で指定した `dist/`) 相対** に変更されている（`@google/clasp@3.3.0` の `build/src/core/clasp.js` L155 + `build/src/core/files.js` L43-62 / L228-232 で確認）。雛形作成時に置いた `**/**` + `!dist/Code.js` 形式のホワイトリストが「dist プレフィックスがマッチしない」ため全ファイル ignore 状態となり、`clasp push` が常に `Script is already up to date.` を返す事象が発生。`apps/google-workspace-addon/.claspignore` を v3 仕様の最小 ignore（`node_modules/**` / `.git/**` / `.DS_Store` / `*.map`）に書き直して恒久 fix。`docs/google-addon-submit-guide.md` / `apps/google-workspace-addon/README.md` に v3 セマンティクス解説とトラブルシュート行を追加。`dist/.claspignore` / `dist/.clasp.json` を別途作る案は破棄（`apps/google-workspace-addon/.clasp.json` の `rootDir: "dist"` 設定でそのまま動くため不要） | PdM Phase A 作業中（Apps Script Editor にファイルが反映されない）の障害解析。clasp v3 ソースを直接読んで原因特定、ネット記事の v2 系手順は混入しやすいため repo 同梱の guide / README を一次資料として更新 |
| 2026-06-17 | **基本設計書 spec.css に章タイトル / Mermaid 図の改頁制御を追加**（6.1 注文確定フロー regression 対応）。`packages/renderer-pdf/src/styles/spec.css` の `.mdb-mermaid` と `.mdb-mermaid svg` に `max-height: 220mm` + `overflow: hidden` を付与（A4 portrait の body 有効高 ≈ 257mm から余白分を差し引いた値）、`.mdb-spec__body > h1 + h2` / `h1 + p + h2` に `break-before: avoid` を付与して h1 単独ページを抑止、`h2/h3 + .mdb-mermaid` / `+ p + .mdb-mermaid` に `break-before: avoid` を付与して図見出しと図の分離を抑止 | PdM スクリーンショット検収（2026-06-17 PM）で「6. 画面遷移 / 6.1 注文確定フロー（顧客側）」付近の調整指示。`templates/spec/standard-ja.md` v0.3.0 配布 PDF を pdftoppm で画像化したところ、p10「6. 画面遷移」だけ・p11「6.1 注文確定フロー」だけの白紙ページが 2 連続、p12-13 で Mermaid フローチャートがページ跨ぎして「マイページ・注文履歴へ」ノードが孤立し 6.2 業務担当者画面が同じページに切れ込む状態を確認。fix 後は同じテンプレートで 15p → 14p に短縮、p10 が「6. + 6.1」同居、p11 にフローチャート全体が 1 ページ収納、p12 が 6.2 独立、p13 が 7. 非機能要件。他章の ER 図（4.1）・コンポーネント図（2.1）・シーケンス図（2.2）に max-height 制約の悪影響なし（regression なし）。Mermaid の図サイズはそのまま、CSS 改頁制御のみで対応 |
| 2026-06-18 | **`apps/google-workspace-addon/` 雛形を新規作成し Phase 2 の土台を確保**。clasp + esbuild（IIFE バンドル + Apps Script trigger 露出 footer 注入）+ TypeScript + vitest 構成。oauth scope は `spreadsheets.currentonly` / `documents.currentonly` / `presentations.currentonly` / `script.container.ui` の **非センシティブのみ**（OAuth 検証申請スキップ可能な最小構成）。PdM 手作業手順は `docs/google-addon-submit-guide.md` に Phase A〜F 時系列で documentation 化、AI 補佐範囲（コード / マニフェスト / Marketplace 草案）と PdM のみ実行範囲（GCP プロジェクト / OAuth consent / clasp login / Marketplace listing 入力 / 検証 / 公開申請）を baseline 15 に従い明確分離 | PdM 指示 2026-06-18「本日の目標 = Google アドオン申請（やったことない）」。Workspace Add-on の Marketplace submit は GCP プロジェクト作成 / OAuth consent screen / Marketplace SDK / 検証申請 / 公開申請の複数日コースで本日 submit 完了は構造的に無理、かつ正本（587147d）の決定で実 submit は `schema-test-spec` (v0.7.0) 完成と同時。本日は土台（コード雛形 + 申請手順書 + ローカル動作確認可能な build）まで進めて、submit 本番は schema-test-spec 完成後に温存する |
| 2026-06-18 | **Google Workspace アドオンの主軸を「md → PDF 出力」ではなく「Docs / Sheets サイドバーでの md ⇔ ドキュメント / スプレッドシート 双方向 UI 編集」とする**。PDF 出力は Google Docs / Sheets / Slides のネイティブ機能（ファイル → ダウンロード → PDF）に委譲。配布チャネルごとの役割分担を明確化: **Chrome 拡張 = PDF 出力が主役（手元 .md ⇔ A4 PDF）**、**Google アドオン = Docs / Sheets / Slides ⇔ md 双方向編集 UI が主役（PDF は Google ネイティブ任せ）** | PdM 決定（2026-06-18 セッション、双方向同期確定の直後）。Google アドオン文脈で改めて PDF 化機能を作ると、Docs / Sheets / Slides 既存の「ファイル → PDF」と機能重複し、ユーザにとって価値が薄い。一方、Sheets 上で検証シートを編集 → md に自動同期 → git で履歴管理、というフローはネイティブには無い純粋な追加価値。Chrome 拡張側は引き続き「ローカル .md を A4 PDF にする」専業で世界観を保ち、配布チャネルが互いに食い合わない設計にする |
| 2026-06-18 | **Phase 2 Google Workspace アドオンを `schema-test-spec`（検証シート）完成と同時に Marketplace submit する**、かつ **双方向同期を必須要件とする**（md ⇔ Google Sheets / Docs / Slides）。第一実装の同期方式は **Sheets-as-truth (A 案)**: Sheets onEdit トリガ → Apps Script → GitHub API 経由で md に自動 commit、md 側の frontmatter に `googleSheetId: <ID>` を埋めて 1:1 紐付け、competing edits は last-write-wins + `_conflict` シートに退避 | PdM 決定（2026-06-18 セッション）。GitHub ネイティブの検証シートは Issue 幅 / 表 + チェックボックスのレンダリング崩れで運用に堪えず、`schema-test-spec` を Google Sheets で運用すれば横幅 / 共同編集 / 認証 / 進捗集計が全てネイティブ機能で解決する。基本設計書は v0.5.0 で出来ており、`schema-test-spec` 完成を待って Marketplace submit する判断。双方向にすることで「Sheets で社内検証 → md で git 履歴化」の運用が成立し、片方向（Sheets → md エクスポート手動）より価値が大幅に高い |
| 2026-06-17 | **Chrome 拡張の表示名を `md-business: Invoice (適格請求書)` → `md-business — Markdown 業務文書ビューワー` に刷新**（manifest.json `name` / `short_name` / `description` を更新、`package.json` の `description` も英訳を v0.5.0 反映に書き換え）。「Invoice」固定では v0.5.0 で同梱した基本設計書テンプレ・Mermaid 図描画・免税事業者モードの範囲をストアカードから読み取れない | 2026-06-14 に決めた「Invoice (適格請求書)」は v0.1.0〜v0.4.0 の請求書一本足スコープに対しては適切だったが、v0.5.0 で `schema-spec`（基本設計書）が独立カテゴリとして popup に並び、Mermaid 図プレビューもメイン機能化した。ストアの 132 文字商品概要 + スクリーンショット 5 枚を見たユーザに対し「請求書専用」と誤認させると、設計書ユースの流入を取りこぼす。シリーズ全体の傘になる呼称を採用 |
| 2026-06-17 | **starter templates の manifest 出力名を `templates/manifest.json` → `templates/index.json` にリネーム**（`apps/chrome-extension/scripts/post-build.mjs` + `apps/chrome-extension/src/popup/index.ts` の fetch URL を同時更新）。Chrome Web Store のパッケージ検証器が zip ルート以下を再帰スキャンして `manifest.json` を複数検出した場合、`複数のマニフェストが見つかりました: manifest.json, templates/manifest.json` でアップロードを拒否するため、拡張本体の manifest.json と衝突する名称を避けた | v0.5.0 zip を Web Store にアップロードした際に上記エラーで弾かれた。MV3 manifest.json は zip ルート以下のどこにあっても候補として扱われる挙動。`index.json` は技術的に同じ JSON だが Web Store の検証器は弾かない。popup の fetch URL も同期で書き換え、後続バージョンでも踏まないようコメントで理由を明記（post-build.mjs L191-193） |
| 2026-06-17 | **v0.5.0 で Mermaid 復活**（id 61 の延期を撤回）。`mermaid@11.15.0` 動的 import + `<pre><code class="language-mermaid">` 検出 short-circuit（請求書フローは初期 bundle 不変、追加 zero-cost）。CSP 違反 chunk は 2 段で除去: ①`vite.config.ts` の `resolve.alias` で `cytoscape` / `cytoscape-cose-bilkent` / `cytoscape-fcose` を `src/shims/cytoscape-empty.ts`（call で throw する空 shim）に差し替え architecture / mindmap chunk を完全に排除 ②同設定の `shimLodashCspViolations()` vite plugin で `Function('return this')()` を `globalThis` に、`x.require('util').types` を `undefined` に置換（lodash-es 本体 + `@mermaid-js/parser` dist にバンドル済みの同等コードの両方）。`mermaid.initialize({ securityLevel: 'strict' })`、`apps/chrome-extension/src/shared/renderMermaid.ts` 87 行 + 4 unit tests、レンダ失敗時はソースを残して `.mdb-mermaid--error` で注釈、PDF にも inline SVG として埋め込まれテキスト検索可。`pnpm build && node scripts/scan-bundle.mjs` clean。動作する: ER / flowchart / sequence（dagre 系レイアウト）/ class / state / journey / gitGraph / timeline / pie / requirementDiagram など。非対応: architecture / mindmap / wardley map | 2026-06-17 早朝の id 61 撤回後、PdM 承認の Option 1（Mermaid subset 自前 build）で MV3 CSP を突破できることが判明。問題 chunk の依存は ER 図描画に不要で、shim と置換で `unsafe-eval` / `require` を完全に除去できた。今後 Mermaid 自体が将来内部実装を変更しても、scan-bundle.mjs が pre-push で検出するため早期に気づける |
| 2026-06-18 | **Phase C-1/C-2 完遂**（テストデプロイ + Sheets サイドバー双方向動作）: 「md → Sheets インポート」「Sheets → md エクスポート」両方向が PdM 手元で動作確証。最終構成: oauth scope = `.currentonly` × 3 + `script.container.ui`、`appsscript.json` から `useLocaleFromApp` 削除（暗黙の `script.locale` 要求を回避）、`addOns.common.logoUrl` は `https://www.gstatic.com/images/branding/product/2x/apps_script_48dp.png`（Google CDN の Apps Script 公式ロゴ、placeholder）に差し替え。OAuth User type は「内部に公開」（Phase F の Marketplace submit 直前に「外部」に戻す）。次は schema-test-spec v0.7.0 で frontmatter スキーマ駆動のデータ検証 / 条件付き書式（OK/NG プルダウン / NG 行薄赤背景 / 日付ピッカー / ヘッダー固定）を実装 → Marketplace submit | PdM Phase C 実走（2026-06-18 13:00〜14:30）。Phase B 途中段階で OAuth consent screen 未作成だと A-4（Apps Script ↔ GCP 紐付け）が拒否される順序制約、Workspace ドメインポリシーが External + Testing 状態のテストユーザー登録を preempt する挙動、Side Panel アイコンが logoUrl 404 で消える挙動などは実走しないと検出不可能だった。失敗パターンと回避策を `docs/google-addon-submit-guide.md` に折り込み済 |
| 2026-06-18 | **`script.container.ui` を Sensitive scope と訂正**。旧版 submit guide では「`.currentonly` 3 つ + `script.container.ui` すべて非センシティブ」と記載していたが、Google の正式分類では `script.container.ui` は **Sensitive scope**（Workspace アドオンとして submit する以上は逃れられない）。Phase E の検証申請で用途文書 + デモ動画が必要になるが、Editor アドオンとしての挙動範囲なのでほぼ自動承認の見込み。`.currentonly` 3 つは引き続き非センシティブ判定で変わらず | submit guide L131 の旧記述「センシティブ判定が最小」が誤誘導になっていた。Phase E（検証申請）の所要時間見積もりに影響する重要な訂正 |
| 2026-06-18 | **「内部に公開」ルートを Workspace ドメインホストの推奨運用にする**: GCP プロジェクトが組織配下（`dokokade.co.jp`）なら、OAuth 同意画面の User type を「内部」に切り替えることで (a) 検証申請不要 (b) テストユーザー登録不要 (c) External + Testing 状態の「アクセス権限をリクエスト」誤動作を回避 ができる。Marketplace 公開（Phase F）の直前に「外部」に戻して submit。個人 Gmail は Internal では使えないが、社内検証なら問題なし | 2026-06-18 PdM 実走時に External + ドメインポリシー preempt で OAuth が「アクセス権限をリクエスト」モードに突入、個人 Gmail（`ladygo755@gmail.com`）から「リクエストを送信」が走り共有依頼メールが業務 Gmail に届いて baseline §17 violation 寸前まで行った。Internal 切替で即座に通った経験から、Workspace ドメインホストのデフォルトルートとして採用 |
| 2026-06-18 | **個人 Gmail からの「アクセス権限をリクエスト」UI は罠**: External + Workspace ドメインポリシーで preempt された OAuth 同意画面で個人 Gmail がアクセスすると「アクセス権限をリクエスト」モードになり、押すと個人 Gmail から業務 Apps Script への共有依頼メールが業務 Gmail 管理者宛に飛ぶ。**「リクエストを送信」は押下禁止 / 受信側は即「不承認」**。baseline §17（非エンジニア owner-task 制約）の延長で、業務インフラに個人アカウントを edit 権で混入させないことを徹底 | submit guide のトラブルシュート行に追記。同セッションで PdM が `ladygo755@gmail.com` から共有依頼を送信 → 業務 Gmail で「不承認」処理 → Internal ルートに切替で恒久対処 |
| 2026-06-18 | **Workspace アドオン `addOns.common.logoUrl` は HTTPS 200 必須**: OAuth consent のロゴとは別に、Sheets / Docs / Slides の右側 Side Panel（縦アイコン列）に出るアイコン URL。404 / private repo の raw URL / 画像でない URL を指定すると **Side Panel アイコン自体が非表示** になり、「テストデプロイしたのに md-business が出ない」事象を引き起こす。自前アイコン未確定の間は `https://www.gstatic.com/images/branding/product/2x/apps_script_48dp.png`（Google CDN）を placeholder として当てておく | submit guide / README の B-2 / トラブルシュート行に追記。2026-06-18 PdM 実走時に `https://raw.githubusercontent.com/meta-taro/md-business/main/docs/assets/addon-logo.png` を指定 → docs/assets/ 未作成で 404 → Side Panel アイコン消失 → 原因特定に 1 時間消費 |
| 2026-06-18 | **`useLocaleFromApp: true` は暗黙の `script.locale` 要求**: Apps Script ランタイムが `useLocaleFromApp` の存在をトリガに `https://www.googleapis.com/auth/script.locale` スコープを要求し、`oauthScopes` に未宣言だと `スクリプトにはその操作を行う権限がありません。必要な権限: https://www.googleapis.com/auth/script.locale` でクラッシュする。**日本語固定運用なら `useLocaleFromApp` を書かない**のが最小スコープ構成。`appsscript.json` 雛形から該当行を削除済。i18n を導入する段で `script.locale`（非センシティブ）を `oauthScopes` に追加 | submit guide の B-3 / トラブルシュート行に追記。2026-06-18 PdM 実走時に発覚 |
| 2026-06-18 | **dokokade.co.jp 業務専用の Chrome プロファイル分離を推奨**: 個人 Gmail（`ladygo755@gmail.com` / `kamikami.customer@gmail.com`）と業務（`tanaka.masatomo@dokokade.co.jp` / `inc@dokokade.co.jp`）を同一 Chrome に同時ログインしているとタブごとに context が flip し、OAuth 同意・clasp ログイン・Apps Script Editor 起動の局面で誤ったアカウントが選ばれて事故が起きやすい。Chrome の「ユーザー追加」で業務専用プロファイルを作成し、業務作業はそのプロファイルでのみ実施するのが恒久対処（緊急対処は不要アカウントの一時ログアウト） | 2026-06-18 PdM 実走時に 4 アカウント同時ログイン状態でアクセスリクエスト誤発火が起きた事案の根本対策。submit guide のトラブルシュート行に追記 |
| 2026-06-18 | **v0.7.0 `schema-test-spec` 設計合意フェーズ着手**: Phase 2 Marketplace submit の最終ゲートとして、検証シート（テスト項目 / 期待結果 / 結果 / 担当 / 実施日 / 備考）の frontmatter スキーマを **Markdown が真** の原則で設計。`columns:` に列ごとの `type` / `values` / `visual` を宣言し、Apps Script が import 時に DataValidation（プルダウン / 日付ピッカー / チェックボックス）+ ConditionalFormat（行全体背景 = `row_background` / セル単独 = `background`）+ setFrozenRows(1) を自動適用。export では `columns` + `visual` を frontmatter として round-trip 復元。型は初版 7 種（`text` / `multiline_text` / `enum` / `date` / `number` / `checkbox` / `url`）。論点詳細・PdM 判断項目は `.claude/issues/002-v0.7.0-schema-test-spec.md` | 2026-06-18 Phase C-1/C-2 完遂直後の PdM 発端（「プルダウンとか、NGなら行を薄い赤背景にするとか可能ですかね？」）が、schema-test-spec の本質を直接表現していた。技術的に Apps Script の DataValidation / ConditionalFormatRule API で完全実装可能であり、frontmatter スキーマ駆動の double-binding として一般化することで「Markdown が真の検証シート」という Phase 2 Marketplace の差別化価値になる |
| 2026-06-18 | **v0.7.0 D-1 確定: シート側のイレギュラーは捨てる/取り込むの二択ではなく「検出して通知」**: Markdown スキーマと Sheets 実データに差分（未知列 / `enum` 値が `values` 外 / `date` 型に非日付 / `number` 型に文字列 等）があったとき、サイドバーに警告 + 該当セルに `setNote` + 行/列背景色で可視化。export 時は通過行のみ md に反映、イレギュラー行は通知のみで commit 保留。`apps/google-workspace-addon/src/lib/validateSheet.ts` 新規 | 2026-06-18 PdM 回答「エラー、イレギュラーなセルを通知したいです」。捨てる（案 X）は実装シンプルだが PdM の編集ミスを silently 消す挙動になり、取り込む（案 Y）は frontmatter 真の原則と矛盾。検出 + 通知は両者のいいとこ取りで、PdM の編集体験が損なわれない |
| 2026-06-18 | **v0.7.0 E-1 確定: onEdit 自動同期を v0.7.0 入り**（v0.7.1 後送りは不採用）。`oauthScopes` に `script.external_request`（**Sensitive**）+ `script.scriptapp`（非 Sensitive）を追加し、Sheets onEdit → debounce 2 秒 → `validateSheet` → 通過行のみ md 再生成 → `UrlFetchApp.fetch` で GitHub `PUT /repos/{owner}/{repo}/contents/{path}` 実行。GitHub PAT は `PropertiesService.getUserProperties` に PdM 手作業で投入（baseline §15）。conflict resolution は v0.7.0 では最終編集勝ち、3-way merge は v0.8.0 以降。トレードオフ: Phase E 検証申請所要日数が 3〜4 週 → **5〜7 週** に延長（Sensitive スコープ 3 個で Google レビュー回数増加） | 2026-06-18 PdM 回答「早めにほしいですね。そうするとシート修正時にエラーが同期されて案内してくれますよね」。Marketplace 公開は遅れるが、検証申請 1 回で済み、PdM 体験は完成度が高い。長期的に合理的選択 |
| 2026-06-18 | **v0.7.0 F-1 確定: パッケージ名は `schema-test-spec`**（音声入力 typo「敗訴の名前」を文脈解釈、違ったら次ターン訂正）。既存 `schema-invoice` / `schema-spec` と命名揃え、roadmap 既載との整合性を維持 | 命名統一原則。命名変更は ripple cost が大きく、確定済の命名規則を維持するのが合理的 |
| 2026-06-18 | **v0.7.0 残論点 A-1/A-2/A-3/B/C-1/C-2/C-3 は推奨案で確定**（PdM 違いがあれば次ターン訂正）: `schema: test-spec/v1` ヘッダ採用 / `columns` 必須化（自動推測 fallback なし）/ `googleSheetId` は同期 ON 時必須に昇格 / 初版型 7 種 / visual キー 2 種（`row_background` / `background`）/ ConditionalFormatRule 実装方針確定 / ラウンドトリップ時は frontmatter 真で常に上書き | 設計合意フェーズの decision velocity を上げるため、技術的な詳細論点は AI が推奨案で確定して進める。PdM のリソースは D-1 / E-1 / F-1 のような戦略レベルの判断に温存する |
| 2026-06-18 | **v0.7.0 Phase 1 TDD GREEN: `packages/schema-test-spec/` 雛形完成**: schema (`test-spec/v1` const + columns minItems:1 必須 + 7 種列型 + enum 条件付き values 必須 + visualStyle hex pattern + additionalProperties:false 全層) / types (`TestSpec` + `ColumnType` + `ColumnVisualStyle` + status union `draft|review|executing|completed`) / dictionary.ja (4 scope: `root/party/column/visualStyle` + STATUS/COLUMN_TYPE/THEME 翻訳) / normalize (column.visual は dict-of-visualStyle 特別扱いで外側 enum キー保持 + 内側 visualStyle scope 再帰) / autofill (defaults + 列名重複/enum without values/visual キー外れ警告) / parseTestSpec (split→normalize→autofill→validateWithCompiled パイプ + DI validate で MV3 CSP 互換) / build-validate.mjs (Ajv standalone + require→import 巻き上げ) を 1 セッションで実装。4 テストファイル 74 件全緑、カバレッジ Stmts 100% Branch 94.87% Funcs 100% Lines 100%（しきい値 80 全 4 軸クリア）。typecheck 通過 | TDD 順序固定 (baseline §4 / §20): plan → 失敗テスト → 実装 → GREEN → typecheck の chain を 1 サイクル分破断なく完走。途中で発生した「column.visual の外側 dict キーが翻訳対象に巻き込まれる」バグもテスト緑/赤の即時 feedback で即修正できた。次は Phase 2: `translateError.ts` + `fileName.ts`（schema-spec / schema-invoice と同パターン）の TDD |
